# Changelog
## Unreleased
- 2026-08-28 18:41 UTC: Stopped the stealth/perf VM-tuning backups from littering `~/Desktop` and made the backup revert-safe in `vfio.sh` (R37: predefined backup dir + keep one pristine per VM):
  - bug (litter + broken revert): the stealth tuner (`install_stealth_vm_tuning`) and the ultimate-perf tuner (`install_ultimate_perf_vm_tuning`) wrote a NEW timestamped VM-XML backup to `$HOME/Desktop` on every run (`win11_stealth_<ts>.xml`, `win11_perf_<ts>.xml`), so the Desktop accumulated dozens of stale backups and never pruned. Worse: a re-tune wrote a backup of the ALREADY-TUNED XML, and `--reset-*-vm-tuning` reverted to the NEWEST backup by mtime — so it restored the tuned XML and revert was a silent no-op. The user called the custom XML-backup mechanism "dumb" and asked to use libvirt's predefined backup instead.
  - research (why NOT libvirt snapshots): investigated `virsh snapshot-create-as` / `virsh snapshot-revert` as the "libvirt-native backup". `snapshot-revert` is DESTRUCTIVE and reverts the DISK image (Red Hat docs: "any changes in the domain since the last snapshot was taken will be lost"; no `--config-only` flag). For a Windows VM with an in-progress AMD driver install, a pre-tuning snapshot would roll back the Windows disk on revert and WIPE the driver install — a real data-loss hazard for this exact VFIO setup (Red Hat also warns memory snapshots are problematic with VFIO passthrough devices). Conclusion: libvirt snapshots are the WRONG tool for config-only revert; the custom XML backup is the correct primitive (it reverts ONLY the VM config XML, never the disk). The real fix is to make it not dumb.
  - fix (4 improvements): (1) default backup dir → predefined `/var/lib/vfio-stealth-vm/backups` and `/var/lib/vfio-perf-vm/backups` instead of `$HOME/Desktop` (the `STEALTH_VM_BACKUP_DIR` / `ULTIMATE_PERF_VM_BACKUP_DIR` conf keys still override; `$HOME/Desktop` is kept only as a legacy READ fallback for backups/owned files created before this change); (2) keep ONE pristine backup per VM — fixed name `${_dom}_stealth.xml` / `${_dom}_perf.xml` (no timestamp), so the dir no longer accumulates; (3) state-aware: new `_vm_is_stealth_tuned` / `_vm_is_perf_tuned` boolean helpers (grep the same markers the status functions use) gate the backup write — only write when the VM is NOT already tuned (current dumpxml is pristine); on a re-tune KEEP the existing pristine backup (so revert still works); if already tuned with NO backup (orphaned), WARN and do NOT fabricate a "pristine" backup from already-tuned XML; (4) revert reads the fixed-name backup directly, falling back to the newest timestamped `*_stealth_*.xml` / `*_perf_*.xml` in the legacy `$HOME/Desktop` dir so EXISTING setups (pre-R37) can still revert. Fixes the latent re-tune-breaks-revert bug. Live-attach backup unchanged (already a fixed per-VM name in `/var/lib/vfio-dynamic`).
  - hugepages owned-file migration (safety-critical): the perf backup dir also holds `${_dom}_perf_hugepages_owned.txt`, which the reboot-persistent boot oneshot + the dynamic at-VM-start ensure sum across ALL dirs in `PERF_HP_DIRS_FILE`. The live setup had the owned file at `/root/Desktop/win11_perf_hugepages_owned.txt` (=16096) with the registry pointing at `/root/Desktop`; after the default-dir change a perf re-tune would write a NEW owned file to `/var/lib/vfio-perf-vm/backups` and register it, while the legacy owned file + registry entry remained → `sum_all_owned` double-counts = 32192 pages (64GB). New `_migrate_legacy_perf_owned <new_dir> <dom>` helper moves the legacy owned file to the resolved dir (or drops the stale copy if the new one already exists) and unregisters the legacy dir from `PERF_HP_DIRS_FILE` when it has no remaining owned files; idempotent. Called in the perf install loop (before reserving) and in `reset_ultimate_perf_vm_tuning` (before restoring) so the owned file is always in the resolved dir.
  - validation: `bash -n` + `shellcheck -S warning` clean on `vfio.sh`; full `regression/script.sh` binder rc=0 (all 13 regression scripts pass, including the in-flight R38 hugepages-strip assertions — R37 layered on top without conflict); all 3 smoke scripts PASS. Extended `regression/dynamic-binding-regression.sh` (R37 stealth block: `_vm_is_stealth_tuned` helper, fixed-name backup, no `$(date)`, pristine-keep, orphan warn, `/var/lib` default, fixed-name revert read + legacy fallback) and `regression/ultimate-perf-regression.sh` (R37 perf block: same + `_migrate_legacy_perf_owned` helper + install/revert migration calls + a functional migration test: moves owned file legacy→new, unregisters legacy dir, idempotent rc 0); added an `assert_contains_text` helper to `ultimate-perf-regression.sh` (it only had `assert_contains_file`). README update skipped per user.
- 2026-08-26 20:18 UTC: Fixed the live-attach (hotplug GPU) opt-out so selecting "no" (or `--no-live-attach`) actually reverts an already-active live-attach setup in `vfio.sh` (R36: live-attach opt-out reverts):
  - bug: in `install_dynamic_binding_from_existing_config`, the RX 9070 live-attach recommendation prompt's "no" branch only printed skip notes and NEVER reverted an already-active live-attach setup — so a user who had live-attach active and re-ran `--install-dynamic-binding` (or the menu "Switch to dynamic binding") and picked "no" was left with live-attach still on the system: the VM XML still had the GPU hostdev stripped (the VM kept booting on a virtual display), the live-attach helper + VM list + per-VM backup stayed on disk, and `VFIO_DYNAMIC_LIVE_ATTACH` stayed `1`. The user reported: "when i run dynamíc and select no for hotplug it still on the system does not switch." The sibling opt-ins (vBIOS `--no-vbios`, stealth `--no-stealth-vm-tuning`) both revert on "no"; live-attach was the inconsistent one.
  - fix: the live-attach block now (a) calls `remove_live_attach` on a prompt "no" — restoring the GPU hostdev to each VM's persistent XML from the per-VM `live-attach-backup-<dom>.xml` backup, detaching the virtio-win ISO CD-ROM, removing the helper/VM-list/device-XMLs/backups, and flipping `VFIO_DYNAMIC_LIVE_ATTACH=0`, so the VM switches back to normal dynamic binding; (b) honors two new override flags `--live-attach` (force the workflow on, regardless of GPU family) and `--no-live-attach` (force skip AND revert any already-active setup), mirroring the `--stealth-vm-tuning`/`--no-stealth-vm-tuning` pattern via a new `LIVE_ATTACH_OVERRIDE` var; (c) gates the RX 9070 recommendation prompt on NO override given, so `--live-attach`/`--no-live-attach` short-circuit it cleanly. The revert runs regardless of GPU family, so the documented "Reversible with `--install-dynamic-binding`" now works for every card, not just RX 9070 / RDNA4.
  - `remove_live_attach` is now best-effort: it always `return 0`s (was returning 1 when nothing was removed, which would trip `set -e` at the bare call sites — the new dynamic opt-out, plus the existing `--install-early-binding` and `--reset` cleanup — when live-attach was never active).
  - validation: `bash -n` + `shellcheck` clean; full `regression/script.sh` binder rc=0; extended `regression/live-attach-regression.sh` (R36 section: override var, 2 parse_args flags, usage one-liner, fish/bash/zsh completions, override force-on/force-off honored, RX9070 prompt gated on no override, prompt-no branch reverts, `>=2` `remove_live_attach` calls in the dynamic installer, `remove_live_attach` returns 0) and `regression/dynamic-binding-regression.sh` (R36: override-0 revert + prompt-no revert + `>=2` calls); fixed a stale R27 smoke assertion in `regression/dynamic-binding-smoke.sh` (R35 had inserted `install-ultimate-perf-vm-tuning | reset-ultimate-perf-vm-tuning |` into the MODE comment between `install-virtio-win-guest-agent` and `menu`, breaking the contiguous substring — the assertion now matches the actual MODE comment); all 3 smoke scripts PASS. README: documented the new `--live-attach`/`--no-live-attach` flags + the "no reverts" behavior (additive, existing text intact).
- 2026-08-26 15:01 UTC: Made the hugepages re-reserve regenerate from the live VM XML and added a dynamic at-VM-start ensure, in `vfio.sh` (R35 follow-up: boot-regenerate + dynamic ensure):
  - motivation: the reboot-persistent boot oneshot (added 2026-08-25 21:22) re-reserved the RECORDED owned count, so it did NOT detect a VM RAM change made between boots; and nothing re-reserved at VM start, so a RAM change made WITHOUT a reboot (edit XML → start VM) still hit qemu's `unable to map backing store for guest RAM: Cannot allocate memory` because the pool was stale. The user asked: "when i start vm does it regerate huge pages att new configured performance at start?" — answer was no (VM start only consumed the pool); this change makes it yes.
  - boot-regenerate: the standalone boot script `vfio-perf-hugepages-reserve.sh` now RECOMPUTES every perf-hugepages owned count from the on-disk libvirt VM XML (`/etc/libvirt/qemu/<dom>.xml`, parsed with a grep `<memory>` extractor — no virsh, since the unit runs `Before=virtqemud.service libvirtd.service` so libvirt is not up yet), updates the owned files, then reserves the sum. A RAM grow/shrink between boots is honored automatically (no manual re-tune after reboot + RAM change). Falls back to the recorded count if the on-disk XML is unreadable. Kept the 1GB guard, verify, no-op-when-satisfied, standalone (never sources vfio.sh).
  - dynamic ensure (`--ensure-domain <dom>`): the same boot script gained a second mode invoked by the libvirt qemu hook at VM `prepare` (bounded by `VFIO_HOOK_HUGEPAGES_TIMEOUT`, default 10s). It reads the VM XML from stdin, recomputes THIS VM's owned count, then ensures `nr_hugepages` >= sum of all owned (grow only; never shrinks at VM start). No-op for non-hugepages VMs (no owned file → exit 0). On a fragmented-RAM shortfall it exits non-zero so libvirt aborts the VM start with a clear error instead of qemu's cryptic `Cannot allocate memory` — and BEFORE the GPU bind, so a failed start leaves the host desktop untouched.
  - hook refactor: `install_libvirt_hook`'s generated `prepare` phase now captures the domain XML from stdin ONCE (`DOMAIN_XML=$(cat)`) so BOTH the hugepages ensure and `vm_uses_guest_gpu` (via `extract_hostdev_bdfs`, which still reads stdin) can use it — consuming stdin twice would silently skip the GPU bind. Added a bounded `_ensure_hp` wrapper; the ensure runs before the live-attach helper launch / `--bind-now`. `vm_uses_guest_gpu` keeps its stdin signature (the existing regression functional test pipes XML to it via stdin, unchanged). `stopped|release` unchanged.
  - validation: `bash -n` + `shellcheck` clean. Extended `regression/ultimate-perf-regression.sh`: fixed the stale no-op assertion (`$total`→`${total}`), added 11 static assertions for the two-mode features (`--ensure-domain`, `QEMU_XML_DIR`, `recompute_owned_from_disk`/`memory_kib`/`pages_needed`/`sum_all_owned` fns, regenerate loop, dynamic grow/abort/grow-only/non-hugepages-no-op), and a functional `--ensure-domain` test (reuses the generated runtime script with sed-redirected paths: owned 1000→4096 recompute, pool 0→4096 grow, exit 0, no-op when satisfied, no-op for a non-hugepages VM). Extended `regression/dynamic-binding-regression.sh` with 5 hook assertions (stdin capture once, `_ensure_hp` wrapper, `--ensure-domain` call, ensure-before-bind abort hook, captured XML piped to `vm_uses_guest_gpu`). Full `regression/script.sh` binder rc=0. README: expanded the boot-reserve bullet (regenerate) + added a "Dynamic re-reserve at VM start" bullet (additive, existing text intact).
- 2026-08-25 21:22 UTC: Made the ultimate-performance hugepages reservation reboot-persistent in `vfio.sh` (R35 follow-up: boot-time systemd re-reserve), fixing `unable to map backing store for guest RAM: Cannot allocate memory` after a host reboot:
  - root cause: a host reboot resets `/proc/sys/vm/nr_hugepages` to 0, but the VM XML still demands `<memoryBacking><hugepages>` (e.g. win11 = 32964608 KiB = 16096 × 2MiB pages). The existing "reboot-safe re-reserve" only fired on the next `--install-ultimate-perf-vm-tuning` RUN — nothing re-reserved the kernel pool at boot — so qemu couldn't mmap the backing store and the VM failed to start until the operator manually re-ran the tuner (or hand-echoed the owned count into `nr_hugepages`). Live-reproduced: post-reboot `nr_hugepages=0` + win11 `start` → `Cannot allocate memory`; the owned file `/root/Desktop/win11_perf_hugepages_owned.txt` (=16096) persisted on disk but the pool was empty.
  - immediate unblock: re-reserved 16096 hugepages from the owned file, verified `HugePages_Total: 16096` / `Hugetlb: 32964608 kB` = win11's full 32GB, so win11 can start now.
  - root-cause fix: added a boot-time systemd oneshot `vfio-perf-hugepages-reserve.service` (`/usr/local/sbin/vfio-perf-hugepages-reserve.sh`) that reads the registry of perf backup dirs (`PERF_HP_DIRS_FILE=/var/lib/vfio-dynamic/perf-hugepages-dirs`), sums the per-VM owned counts (`*_perf_hugepages_owned.txt`), and re-reserves the pool *before* libvirt starts autostart VMs (`Before=virtqemud.service libvirtd.service`, `ConditionPathExists` on the registry, `WantedBy=multi-user.target`). Honors the 1GB-page guard (skip + warn), verifies the kernel delivered the full count (warns on a fragmented shortfall without reverting — a partial reservation is better than none at boot), and no-ops when the pool already satisfies the owned counts (idempotent). The boot script is STANDALONE (never sources vfio.sh — at boot `/usr/local/bin/vfio` may be absent and sourcing would re-parse args). Installed + enabled + started-once-now automatically by `install_ultimate_perf_vm_tuning` whenever there is perf-hugepages ownership (this run reserved OR a prior run's registry still lists owned files — covers upgrading an existing hugepages setup that predates the service).
  - registry helpers: new `_register_perf_hugepages_dir()` / `_unregister_perf_hugepages_dir()` (dedup-safe append / remove-and-prune-when-empty). `_reserve_host_hugepages_for_vm` registers the backup dir after writing the owned file; `_restore_host_hugepages_for_vm` unregisters it when its last owned file is removed. `reset_ultimate_perf_vm_tuning` removes the boot service once the registry is gone (last hugepages VM reverted); `--reset` intentionally LEAVES the service in place (it does not revert perf VM XMLs, so removing the re-reserve would break a hugepages-backed VM across reboots) and now points the operator at `--reset-ultimate-perf-vm-tuning` for the full perf + hugepages + boot-service cleanup.
  - validation: `bash -n` clean; `shellcheck` clean (vfio.sh + regression scripts). Extended `regression/ultimate-perf-regression.sh` with a Reboot-persistent re-reserve section: static wiring (3 constants, 4 helpers/functions, reserve/restore register/unregister calls, install+remove wiring, ownership + removal guards, boot-script content [standalone, REGISTRY, 1GB guard, shortfall warn, nr_hugepages write], boot-unit content [ConditionPathExists, Before libvirt, WantedBy, oneshot, ExecStart], `--reset` leaves-it-in-place + points to `--reset-ultimate-perf-vm-tuning`) + a functional test that GENERATES the real runtime boot script via `install_perf_hugepages_boot_service` (temp paths, no-op systemctl, non-root write_file_atomic) and `bash -n`s it (stronger than bash -n on the raw source, which mis-parses the backslash-dollar markers inside command substitution) + a registry-helper round-trip (dedup, add second dir, unregister leaves the other, last unregister removes the registry file, no-op unregister on a missing registry). Extended `regression/ultimate-perf-smoke.sh` with a matching generate-and-bash-n smoke. Full `regression/script.sh` binder rc=0. README: added a "Boot-time auto re-reserve (reboot-persistent)" bullet to the hugepages hardening list (additive, existing text intact).
- 2026-08-25 21:00 UTC: The dynamic binding installer and the full wizard's dynamic path now offer ultimate-performance VM tuning (opt-in, default N) in `vfio.sh`, mirroring the stealth tuning opt-in:
  - root cause: the dynamic binding switcher (`install_dynamic_binding_from_existing_config`, used by `--install-dynamic-binding` + menu option 2) and the full wizard's dynamic path (`apply_configuration` when `binding_mode=DYNAMIC`) both prompted for stealth/perf VM tuning but NEVER offered the separate ultimate-performance layer — so a dynamic install applied stealth but silently skipped ultimate-perf, forcing a separate `--install-ultimate-perf-vm-tuning` run afterward. The user reported "dynamic installer does not call performance tuning."
  - fix: added an ultimate-perf opt-in block to BOTH dynamic paths, placed right after the stealth tuning opt-in. Honors `ULTIMATE_PERF_VM_TUNING_OVERRIDE` (1=force on, 0=force skip, empty=prompt default N). Calls `install_ultimate_perf_vm_tuning` (which handles its own internal hugepages + SATA→virtio opt-in prompts). Selecting "no" just skips (reverting ultimate-perf is a separate explicit action: `--reset-ultimate-perf-vm-tuning`). New `--ultimate-perf-vm-tuning` / `--no-ultimate-perf-vm-tuning` flags (parse_args, usage one-liner, help blocks, fish/bash/zsh completions) + `ULTIMATE_PERF_VM_TUNING_OVERRIDE` var mirror the `--stealth-vm-tuning` / `--no-stealth-vm-tuning` pattern.
  - validation: `bash -n` clean. Extended `regression/ultimate-perf-regression.sh` with a Dynamic-install integration section: static asserts (override var, 2 parse_args flags, usage, fish + zsh completions) + a robust indent-based count proving BOTH the switcher (4-space indent, 2 calls) and the full wizard dynamic path (6-space indent, 2 calls) wire in `install_ultimate_perf_vm_tuning` + honor the override (no fragile awk function-region extraction — heredocs inside those functions break region slicing). rc=0. README: added an "Offered during dynamic binding setup" paragraph (additive, existing text intact).
- 2026-08-25 20:30 UTC: Added an opt-in SATA→virtio-blk disk-bus conversion to the ultimate-performance VM tuning in `vfio.sh`, so the disk perf knobs (cache=none/io=native/discard=unmap + multiqueue + iothread) apply to a SATA boot disk too:
  - motivation: the R35 perf patcher's disk loop only touches virtio/scsi disks — a SATA boot disk (common on Windows VMs created before virtio drivers were installed) got NONE of the disk perf knobs. The user asked for the script to convert SATA disks to virtio and tell them to install the driver first.
  - new opt-in: `--ultimate-perf-virtio-disk` / `--no-ultimate-perf-virtio-disk` flags (parse_args, usage one-liner, help blocks, fish/bash/zsh completions) + `ULTIMATE_PERF_VIRTIO_DISK_OVERRIDE` var + `ULTIMATE_PERF_VIRTIO_DISK` conf key (with WHY note). Default OFF (a plain run never changes the disk bus). The install function prompts (default N) with a STRONG `DANGER:` warning that Windows needs the virtio-blk (viostor) driver installed FIRST or it BSODs with `INACCESSIBLE_BOOT_DEVICE`, and tells the user the correct sequence: `--install-virtio-win-guest-agent` → boot → run `virtio-win-guest-tools.exe` → shut down → THEN tune with `--ultimate-perf-virtio-disk`.
  - python patcher pre-pass: when `VFIO_PERF_VIRTIO_DISK=1`, a new pre-pass (before the existing disk I/O perf loop) converts each non-cdrom disk whose bus is NOT virtio/scsi to `bus='virtio'`: target dev `sdX`→`vdX` (collision-checked against existing vdX targets), `bus`→`virtio`, and the SATA/AHCI `<address type='drive'>` is dropped (bus-specific; libvirt reassigns a PCI address on define). Preserves boot order, serial, source. The existing perf loop then applies the knobs to the now-virtio disk. CDROMs are never converted. Idempotent (a re-run on an already-virtio disk is a no-op → exit 3).
  - per-VM success line: when conversion is on, prints `✔ disk bus converted SATA->virtio-blk (sdX->vdX) + cache=none + io=native + discard=unmap + multiqueue`; when off, prints the original line with a `(virtio/scsi disks only)` qualifier so the user knows SATA disks were skipped.
  - validation: `bash -n` clean; embedded python `py_compile` clean. Functional mock test: an 8GiB SATA-boot-disk VM with `VFIO_PERF_VIRTIO_DISK=1` → boot disk converts `sda`/`sata`→`vda`/`virtio`, drive address removed, `cache="none" io="native" discard="unmap" queues="4" iothread="1"` applied, cdrom stays `sdb`/`sata`, disk serial + boot order preserved, `virt-xml-validate` passes, all stealth markers survive, idempotent (exit 3 on re-run). With `VFIO_PERF_VIRTIO_DISK=0` the SATA disk is untouched (no conversion, no disk knobs). Collision-safe: a mock with an existing `vda` + a SATA `sda` → `sda` converts to `vdb` (not `vda`). Extended `regression/ultimate-perf-regression.sh` (11 static + 13 functional assertions for the conversion, rc=0) and `regression/ultimate-perf-smoke.sh` (SATA conversion + idempotency + opt-out, rc=0). README: new "SATA→virtio-blk disk conversion" paragraph + code example + conf-key mention (additive, existing text intact).
- 2026-08-25 20:00 UTC: Fixed a design flaw in the guest-GPU VM detection used by the stealth/perf tuners in `vfio.sh` — a live-attach VM (the recommended RDNA4 path) could not be tuned without reverting live-attach first:
  - root cause: `install_stealth_vm_tuning()`, `reset_stealth_vm_tuning()`, `stealth_vm_tuning_status()`, `install_ultimate_perf_vm_tuning()`, `reset_ultimate_perf_vm_tuning()`, and `ultimate_perf_vm_tuning_status()` all detected a guest-GPU VM by grepping the GPU PCI hostdev BDF out of the VM's persistent XML. But live-attach strips the GPU hostdev from the persistent XML (so the VM boots without the GPU and hot-attaches it later), so the BDF grep found nothing and the tuner reported "No shut-off VMs with the guest GPU found" — even though the VM clearly has the GPU (it's just hot-attached at runtime). The perf/stealth knobs (disk I/O, iothreads, cputune/numatune, topology, SMBIOS, NIC, etc.) have nothing to do with the GPU hostdev, so forcing a live-attach revert just to tune was the wrong UX. `install_virtio_win_guest_agent()` already had the correct fallback (BDF match OR in the live-attach VM list) with a comment explaining exactly this — the tuners just never got it.
  - fix: extracted the proven virtio-win detection pattern into a shared main-scope helper `_vm_is_guest_gpu_vm()` (inserted before `_vm_memory_kib()`): a VM qualifies if its XML has the GPU PCI hostdev (BDF match) OR it is in `/var/lib/vfio-dynamic/live-attach-vms`. Accepts an optional pre-fetched dumpxml as `$2` (avoids a redundant `virsh dumpxml` when the caller already has the XML). `VFIO_LIVE_ATTACH_VM_LIST` env var overrides the list path for regression testing. All 6 tuner/status functions (stealth install/revert/status + ultimate-perf install/revert/status) now route through this helper instead of their own inline BDF-only awk parser. The R35 "nothing to tune" message now also names the live-attach VM list (mirroring the virtio-win installer message).
  - validation: `bash -n` clean; `shellcheck -S warning` clean (the now-unused `_bdfs` locals are SC2034 style-level, below warning). Extended `regression/ultimate-perf-regression.sh` with a Live-attach-aware detection section: static asserts (helper exists, honors `VFIO_LIVE_ATTACH_VM_LIST`, falls back to the list, R35 install uses the helper, "nothing to tune" message mentions the list) + 3 functional tests with a self-contained temp `CONF_FILE` + temp live-attach list (BDF match -> 0, no-BDF + in-list -> 0, no-BDF + not-in-list -> 1), no virsh/root needed (helper takes the XML as `$2`). rc=0. README: added a "Live-attach aware" paragraph to the Ultimate-performance section (additive, existing text intact).
- 2026-08-25 19:40 UTC: Hardened the ultimate-performance hugepages reservation in `vfio.sh` (R35 follow-up: reserve-first + verify, RAM-change aware, reboot-safe re-reserve, 1GB guard, KiB-units fix):
  - **units fix (critical)**: the `<memoryBacking><hugepages><page>` element now emits `unit="KiB"` (was `unit="MiB"`) to match libvirt's XML convention (2048 KiB = 2 MiB = the standard 2MB hugepage). The old `MiB` made libvirt request 2048 MiB pages, which the kernel's `nr_hugepages` 2MB pool never satisfies. Conf key `ULTIMATE_PERF_HUGEPAGES_SIZE` is now documented as KiB (default `2048`); the helper param `_size_mib`→`_size_kib` (`_page_kib="$_size_kib"`); display lines divide by 1024 for MiB; README corrected from "default 2048 MiB" to "default 2048 KiB = 2 MiB".
  - **reserve-first + verify**: the host `nr_hugepages` reservation now runs BEFORE the python patcher (was after), and the helper re-reads `/proc/sys/vm/nr_hugepages` after the write to confirm the kernel delivered the full count. On a shortfall (fragmented RAM) it reverts the pool and returns 1 → the caller sets `_hp_vm=0` → the python skips `<memoryBacking>` so the VM keeps all other perf knobs and still starts, with a WARN naming the shortfall + the `hugepages=N` kernel-cmdline fallback (reboot-safe, can't be fragmented out).
  - **RAM-change aware**: the page count is recomputed from the VM's CURRENT `<memory>` each run via new `_vm_memory_kib()` (grow = add, shrink = free), instead of only ever adding. The count THIS VM owns is persisted to `${vm}_perf_hugepages_owned.txt`; target = sum of OTHER tuned VMs' owned + this VM's new need, so a RAM shrink frees exactly this VM's surplus while preserving other tuned VMs' reservations.
  - **reboot-safe re-reserve**: on a re-run where the VM already has `<hugepages>` but the host pool is 0 (post-reboot), the helper re-reserves instead of skipping, so `nr_hugepages=0` after a reboot no longer silently breaks `virsh start`.
  - **1GB guard**: `ULTIMATE_PERF_HUGEPAGES_SIZE >= 1048576` (KiB) → warns that 1GB pages need `hugepagesz=1G hugepages=N` on the kernel cmdline and skips the runtime write (the `nr_hugepages` knob only controls the 2MB pool), so no mismatched `<memoryBacking>` is added.
  - **rollback on skip paths**: on already-tuned/validate-fail/user-decline/define-fail, `_restore_host_hugepages_for_vm` removes the ownership file + recomputes the pool (except the idempotent already-tuned path which keeps ownership, since the VM still has hugepages).
  - validation: `bash -n` clean; embedded python `py_compile` clean; the patcher produces `virt-xml-validate`-passing XML on a mock 8GiB VM with `<page size="2048" unit="KiB">`, 4096-page math (8388608/2048), all stealth markers surviving, idempotent (exit 3 on re-run). Updated `regression/ultimate-perf-regression.sh` (fixed the stale `unit="MiB"` assertion → `unit="KiB"`; added a Hugepages-hardening section: KiB units + no-MiB-leak, `_vm_memory_kib` reads 8GiB→8388608 KiB + zero→0, RAM-change math 8GiB→4096 + 4GiB→2048, 1GB guard threshold+warn, reserve-first+verify documented + re-read + shortfall check, reboot-safe re-reserve, owned-file accounting, rollback-on-skip; FAIL SUMMARY preserved, rc=0) and `regression/ultimate-perf-smoke.sh` (added a hugepages-ON run asserting KiB units + stealth survives + validates, rc=0). README hugepages paragraph expanded additively (KiB units, reserve-first+verify, RAM-change aware, reboot-safe, 1GB guard) + the `ULTIMATE_PERF_HUGEPAGES_SIZE` default corrected from MiB to KiB.
- 2026-08-25 18:40 UTC: Added the ultimate-performance VM tuning installer (stealth-safe) to `vfio.sh` — a SEPARATE, more aggressive perf layer than the basic perf subset bundled inside `install_stealth_vm_tuning()`, applied to each shut-off guest-GPU VM while NEVER touching the stealth elements:
  - new `install_ultimate_perf_vm_tuning()` + `reset_ultimate_perf_vm_tuning()` + `ultimate_perf_vm_tuning_status()` (inserted after `stealth_vm_tuning_status()`), with two hugepages helpers `_reserve_host_hugepages_for_vm()` / `_restore_host_hugepages_for_vm()`. The perf python patcher (idempotent, exit 3 = no changes) touches only perf elements: disk `cache=none`+`io=native`+`discard=unmap` + virtio-blk multiqueue `queues=<vcpu>` + per-disk `iothread` round-robin; `<iothreads>` scaled to `min(vcpu,4)`; aggressive `cputune` (vcpupin/emulatorpin/iothreadpin from host topology) + `numatune` (memory `nodeset` strict-pinning ONLY when NUMA-safe — single node or all pinned vCPUs fit one node — otherwise skipped so the VM still starts; falls back to simple v->v pinning if host topology unreadable); CPU `<topology>` matching vCPU count + `<cache mode='passthrough'/>`; `currentMemory=memory` (no startup balloon, synergizes with stealth's `memballoon=none`); `<pm>` S3/S4 disabled. Hugepages is the ONLY host-RAM knob, OPT-IN (prompted default N, or `--ultimate-perf-hugepages`/`--no-ultimate-perf-hugepages`): adds `<memoryBacking><hugepages>` + reserves host `nr_hugepages` (prior value backed up to `${vm}_perf_hugepages_prev_<ts>.txt`, reverted by `--reset-ultimate-perf-vm-tuning`). AMD-gated; only shut-off VMs; `virt-xml-validate` + `prompt_yn` per VM before `virsh define`; `--dry-run` shows a `diff -u`.
  - **stealth coexistence guarantee**: the perf patcher never reads/writes `vendor_id`, `kvm/hidden`, `vmport`, `os/smbios`, `sysinfo`, `interface/model` (e1000e), `disk/serial`, `memballoon`, `clock/timer` (hypervclock/tsc), or `qemu:commandline` (-cpu/-smbios); it registers the `qemu`+`libosinfo` namespaces so ElementTree does not rewrite them to `ns0`. A stealth-tuned VM stays stealth-tuned through a perf pass; reverting perf restores a pre-perf backup that still contains stealth.
  - full CLI wiring mirroring the stealth pattern: new flags `--install-ultimate-perf-vm-tuning`, `--reset-ultimate-perf-vm-tuning`, `--ultimate-perf-hugepages`, `--no-ultimate-perf-hugepages`; MODE comment, `ULTIMATE_PERF_HUGEPAGES_OVERRIDE` var, fish/bash/zsh completions, usage one-liner + help blocks, parse_args, main dispatch, 2 new `--menu` entries (Apply/Revert ultimate-perf, renumbered Exit 12→14), `--detect`/`--verify` status block; 3 conf keys (`ULTIMATE_PERF_VM_BACKUP_DIR`, `ULTIMATE_PERF_HUGEPAGES`, `ULTIMATE_PERF_HUGEPAGES_SIZE`) with WHY notes.
  - validation: `bash -n` + `shellcheck -S warning` clean; embedded python `py_compile` clean; the patcher produces `virt-xml-validate`-passing XML on a mock VM (with stealth markers present), preserves all stealth markers (vendor_id, -cpu arg, disk serial, e1000e, memballoon=none, vmport off, hypervclock/TSC, qemu:commandline prefix), is idempotent (exit 3 on re-run), and hugepages opt-in adds `<memoryBacking>` + still validates. New `regression/ultimate-perf-regression.sh` (static wiring + functional python assertions + scoped numatune `nodeset`-not-`cpuset` check + FAIL SUMMARY, rc=0) and `regression/ultimate-perf-smoke.sh` (extract patcher + mock XML + virt-xml-validate + stealth survives + idempotency, rc=0); updated the R27 MODE-comment contiguous-substring assertion in `regression/live-attach-regression.sh` for the new `install-ultimate-perf-vm-tuning | reset-ultimate-perf-vm-tuning |` tokens. README: new `### Ultimate-performance VM tuning (stealth-safe)` subsection + Quick links + TOC + intro teaser (additive, existing text intact).
- 2026-08-12 21:14 UTC: Added a `.gitignore` to keep vBIOS `*.rom` dumps out of version control:
  - vBIOS dumps are large binary blobs (~2MB each) and card-specific (each user dumps their own GPU's vBIOS into `VBIOS/` for the auto-injection feature), so they are not source code and should not be committed; the matcher (`_vbios_rom_matches_gpu`) reads each dump's own embedded PCI Vendor:Device ID, so a non-matching dump is never used regardless of filename,
  - the `.gitignore` ignores `VBIOS/*.rom` (scoped to the vBIOS folder) plus a fallback `*.rom` (any stray dump elsewhere in the repo), while keeping `VBIOS/README.md` tracked (it documents how to obtain a dump); verified a `VBIOS/test-ignore.rom` file shows as `!!` (ignored) under `git status --ignored` and `.gitignore` itself shows as `??` (new untracked file ready to add),
  - no source code changed; `VBIOS/tuf-gaming.rom` was already untracked (its deletion was committed in a prior commit), so this only prevents future dumps from being accidentally `git add`ed.
- 2026-08-12 21:10 UTC: Made the vBIOS ROM auto-injection section always print a prominent header + the guest GPU's PCI ID + the techpowerup Device Id serial + a deep link, and made every skip path a clear WARN, in `vfio.sh` (`install_vbios_romfile()`):
  - previously the `==== vBIOS ROM auto-injection ====` section header + PCI ID + techpowerup URL only printed on the SUCCESS path (a matching `.rom` was found); the skip paths (no `VBIOS/` folder, no `*.rom` files, no candidate match) only emitted quiet dim `note` lines with no header, so a user who had not yet dropped a `.rom` in the folder would see a few easy-to-miss dim lines and no section banner — and the success path's match description said "verify by eye" but never printed the URL to actually do so,
  - now the section header + `Guest GPU <BDF> PCI ID: <vendor:device>` + `techpowerup Device Id for vBIOS lookup: <vendor-device-subvendor-subdevice>` + `Find/verify a vBIOS dump at: <url>` print ONCE, up front, right after the guest GPU's PCI IDs are read from sysfs, so EVERY path below (no folder / no `.rom` files / no match / successful match) leaves the operator with the concrete serial + URL needed to find or verify a dump — the techpowerup Device Id serial (e.g. `1002-7550-1043-0614`) is now printed as a standalone copy-pasteable string alongside the deep link so it can be pasted directly into techpowerup's vgabios search box,
  - the three skip paths are now `WARN:`-prefixed (e.g. `WARN: no *.rom files found in <dir>; vBIOS ROM auto-injection skipped (no dump to pin).`) instead of quiet `note` lines, so a missing vBIOS dump is obvious in the install log instead of being a few dim lines that scroll by — the no-match path also repeats the techpowerup Device Id + URL so the operator can go find the right dump without scrolling back up,
  - the success path now also benefits from the up-front URL (the "verify by eye" match description is now actionable: the operator can click the already-printed techpowerup deep link to verify the best-effort family match is the right board/SKU),
  - the subsystem vendor/device IDs are read from sysfs (`/sys/bus/pci/devices/<BDF>/subsystem_vendor` + `subsystem_device`) and combined with the config-space vendor/device into the 4-part techpowerup Device Id; if the subsystem IDs are unreadable it falls back to the 2-part `vendor-device` form,
  - updated the `regression/dynamic-binding-smoke.sh` Q3z static assertion to verify the new up-front strings (`Find/verify a vBIOS dump at: $_tpu_url`, `techpowerup Device Id for vBIOS lookup: $_tpu_did`) AND the new `WARN: no *.rom files found` / `WARN: no VBIOS/ folder` skip strings are present (the old assertion checked for the now-removed `Find one at: $_tpu_url` line); the live `_vbios_techpowerup_url` deep-link test (simulated sysfs -> `?did=1002-7550-1043-0614`) is unchanged and still passes; `bash -n` + `shellcheck -S warning` clean in the edited region; `dynamic-binding-regression.sh` = "Dynamic binding regression checks passed." (no vBIOS string assertions there, so unaffected).
- 2026-08-12 21:00 UTC: Fixed a `set -euo pipefail` abort that skipped the stealth/perf VM tuning step during `--install-dynamic-binding --stealth-vm-tuning` in `vfio.sh`:
  - root cause: `install_vbios_romfile()` runs a `python3` heredoc that calls `sys.exit(3)` to signal "no changes needed" when a shut-off VM already has the vBIOS `<rom file=.../>` wired in; because `set -e` is active and the `python3` call was NOT in a conditional context, bash aborted the whole script with exit 3 immediately — before `local _py_status=$?` could run — so the script died right after "Installed vBIOS dump to ..." and never reached the stealth tuning step (step 8) that follows `install_vbios_romfile()` in `install_dynamic_binding_from_existing_config()`,
  - the same latent `set -e` hazard existed in `install_stealth_vm_tuning()` (python exit 3 = "already tuned") and `remove_vbios_romfile()` (python exit 3 = "nothing to remove"), so once the vBIOS step was unblocked, a VM that was already tuned would have killed the script the same way in the stealth step,
  - fix: capture the python3 exit status via `|| _py_status=$?` in all three heredocs (declared `local _py_status=0` before the call, appended `|| _py_status=$?` to the heredoc invocation, removed the now-redundant `local _py_status=$?` after it) so the non-zero exit is "handled" and `set -e` does not fire, while the existing `if (( _py_status == 3 ))` / `== 4` / `!= 0` branches keep their original semantics (3 = no changes needed, 4 = unsupported/not found, other non-zero = real patching failure),
  - also fixed a stale smoke-test assertion in `regression/dynamic-binding-smoke.sh` (Q3u case 5) that expected the old `install_hypervisor_hiding()` call to be wired into `install_dynamic_binding_from_existing_config`, but that call was intentionally replaced by `install_stealth_vm_tuning()` (which supersedes it with a realistic OEM `vendor_id=GENUINE00000` instead of `random`, avoiding a duplicate vendor_id) — the assertion now verifies the actual architecture: `install_stealth_vm_tuning` is defined + wired into the dynamic installer, uses `GENUINE00000`, and the old `install_hypervisor_hiding` call is NOT present in the dynamic path; the persistent `dynamic-binding-regression.sh` already had the correct Q3v assertions so it was unaffected,
  - validation: `bash -n` clean on `vfio.sh` and `regression/dynamic-binding-smoke.sh`; `shellcheck -S warning` clean in the three edited heredoc regions (no new SC2181/SC2086); `regression/dynamic-binding-smoke.sh` = SMOKE SUMMARY: PASS (all cases including Q3u now pass); `regression/dynamic-binding-regression.sh` = "Dynamic binding regression checks passed." (all Q3u/Q3v/stealth assertions PASS, including `Q3v stealth tuning handles python exit 3 (no changes)` which confirms the `_py_status == 3` branch is preserved).
- 2026-08-10 18:37 UTC: Added vmport state=off + full-wizard stealth prompt to `vfio.sh`:
  - the stealth python tuning now sets `<vmport state='off'/>` in the `<features>` block — this disables the VMware backdoor port (I/O port 0x5658 'VMX') that Windows detection tools probe to report "Is Virtual Machine: yes"; combined with the existing CPUID hypervisor-bit disable + hyperv vendor_id spoofing, this closes the last major hypervisor detection vector in the VM XML (functional smoke verified: input `state='on'` → output `state='off'`),
  - the full wizard (`apply_configuration()`) now asks the user if they want stealth/perf VM tuning when dynamic binding is chosen — previously this prompt only existed in the standalone `--install-dynamic-binding` switcher, not in the full install wizard; the full wizard's dynamic block now also installs the reboot-FLR monitor (which was also missing from the full wizard, only present in the standalone switcher) and runs the same `STEALTH_VM_TUNING_OVERRIDE` opt-in prompt (default N, honors `--stealth-vm-tuning`/`--no-stealth-vm-tuning`), so a first-time `sudo ./vfio.sh` install that picks dynamic binding now gets asked "Apply stealth/perf VM tuning to detected guest-GPU VMs?" with vmport off mentioned in the prompt,
  - `bash -n` + `shellcheck` + `python py_compile` clean; full `regression/script.sh` suite = 17 PASS / 0 FAIL with 5 new Q3v+ assertions (vmport.set state off, full wizard calls install_reboot_flr_monitor, full wizard calls install_stealth_vm_tuning, full wizard prompts for stealth tuning, prompt mentions vmport off).
- 2026-08-10 14:10 UTC: Added 7 improvements to the stealth/perf VM tuning in `vfio.sh` (building on the 13:55 absorption of the MIT-licensed Stealthy-VM tuning by Fredrik Bäckström):
  - #1 host SMBIOS from DMI: `install_stealth_vm_tuning()` now reads the host's real DMI identity from `/sys/class/dmi/id/{sys_vendor,product_name,bios_vendor,bios_version,bios_date}` and exports it as `VFIO_STEALTH_DMI_*` env vars for the python tuning, so the spoofed SMBIOS type 0 (BIOS) + type 1 (system) match the actual host hardware instead of a generic ASUS B550 / AMI 1802 (falls back to the hardcoded defaults if DMI is unreadable); the bash wrapper logs which DMI values it is spoofing,
  - #2 preserve existing `<qemu:commandline>` args: replaced `reset_qemu_commandline()` (which wiped ALL existing QEMU args) with `get_or_create_qemu_commandline()` + `remove_qemu_arg_pair()` that dedupe only the stealth `-cpu`/`-smbios` pairs, so user args like Looking Glass `kvmfr` / vendor-specific args survive the tuning; re-running is still idempotent (no duplicated `-cpu`/`-smbios`); functional smoke verified a `kvmfr0` arg survives both the first run and a re-run,
  - #3 `--reset-stealth-vm-tuning` standalone revert mode: new `reset_stealth_vm_tuning()` function + MODE that finds each guest-GPU VM's most recent `*_stealth_*.xml` backup and `virsh define`s it back (validates with `virt-xml-validate`, prompts before redefine, skips running VMs); added `--reset-stealth-vm-tuning` to parse_args, usage/help, and fish/bash/zsh completions, plus a standalone MODE dispatch; `reset_vfio_all()` now prints a note that stealth-tuned VM XMLs are NOT reverted by `--reset` (use `--reset-stealth-vm-tuning`),
  - #4 stealth status in `--detect` / `--verify`: new `stealth_vm_tuning_status()` helper that checks each guest-GPU VM's XML for `vendor_id=GENUINE00000` + QEMU `-cpu`/`-smbios` args and prints a per-VM "stealth-tuned" / "NOT stealth-tuned" status line; wired into both `detect_existing_vfio_report()` (detect report, before "==== End report ====") and the verify function (before the RESULT line),
  - #5 `--dry-run` diff: `install_stealth_vm_tuning()` now shows a `diff -u` of the current vs tuned XML per VM (before the redefine prompt) when `--dry-run` is set, so `--install-stealth-vm-tuning --dry-run` previews the exact XML changes,
  - #6 configurable `STEALTH_VM_BACKUP_DIR`: new conf key (default empty = `$HOME/Desktop`) persisted by `write_conf()`; `install_stealth_vm_tuning()` / `reset_stealth_vm_tuning()` read it from conf, fall back to `$HOME/Desktop`, then to `/var/lib/vfio-stealth-vm/backups` if the chosen dir is not writable; logs which backup dir is in use,
  - #7 README subsection: added a `### Stealth/perf VM tuning (SMBIOS / CPU / NIC / disk serials / iothreads)` subsection under "What's new" with a Quick links entry, documenting what it does (hypervisor hide, CPU, DMI-driven SMBIOS, devices, perf, preserves commandline), the verify-before-define safety flow, the `--stealth-vm-tuning`/`--no-stealth-vm-tuning`/`--install-stealth-vm-tuning`/`--reset-stealth-vm-tuning` flags, the `STEALTH_VM_BACKUP_DIR` key, and the MIT license attribution; additive, existing text intact,
  - validation: `bash -n` + `shellcheck` clean on `vfio.sh`; embedded python compiles (`py_compile`); functional smoke verified #1 (Gigabyte/X670E/F32 SMBIOS from env vars) + #2 (kvmfr0 arg preserved through tune + re-tune) + idempotency (no duplicated `-cpu`/`-smbios`); full `regression/script.sh` suite = 17 PASS / 0 FAIL with ~30 new Q3v+ assertions covering all 7 improvements (DMI reads, preserve commandline, reset mode CLI/usage/completions/dispatch, detect/verify status, dry-run diff, backup dir, README); also fixed a parse_args case-label mangling (`--stealth-vm-tuning)` label had been wrongly set to `--install-usb-bt-mitigation)`).
- 2026-08-10 13:55 UTC: Absorbed the Stealthy-VM stealth/perf VM XML tuning into `vfio.sh` (option 3 — the tuning is now available directly from vfio.sh, applied to detected guest-GPU VMs at install time with verification before proceeding). The Stealthy-VM tuning is MIT-licensed (Copyright (c) 2026 Fredrik Bäckström); the algorithm is ported faithfully with attribution:
  - new `install_stealth_vm_tuning()` function that ports the Stealthy-VM `setup_vm_stealth.sh` python3 ElementTree tuning (profile=all: stealth + perf) into a heredoc'd invocation: hyperv `vendor_id=GENUINE00000` + `kvm hidden`, CPU `host-passthrough` + `hypervisor` CPUID-bit disable, SMBIOS type 0 (BIOS) + type 1 (system) spoofing with randomized serial/UUID, virtio→e1000e NIC, randomized disk serials, `memballoon=none`, `hypervclock` off, TSC native, QEMU `-cpu`/`-smbios` commandline args, plus perf (iothreads=1, host-aware cputune, disk iothread assignment); idempotent (rebuilds `<qemu:commandline>` from scratch),
  - detects guest-GPU VMs the same way `install_hypervisor_hiding()` does (virsh list --all --name, parse PCI hostdev BDFs, match GUEST_GPU_BDF from conf); only acts on shut-off VMs (running VMs are skipped with a note); AMD-gated (consistent with the existing hypervisor hide),
  - **verification before proceeding**: for each guest-GPU VM, dumps + backs up the XML, runs the python tuning on a temp copy, validates the tuned XML with `virt-xml-validate`, and `prompt_yn` (default N) before `virsh define`; if validation fails or the user declines, that VM is skipped (non-fatal); python exit 3 (no changes needed) is handled as "already tuned" success; python3 availability is checked,
  - supersedes `install_hypervisor_hiding()` in the dynamic install path (`install_dynamic_binding_from_existing_config`): the old `install_hypervisor_hiding()` call is replaced by an opt-in block gated on `STEALTH_VM_TUNING_OVERRIDE` (1=force on, 0=force skip, empty=prompt default N); `install_hypervisor_hiding()` is kept as a standalone fallback helper but no longer called from the dynamic path to avoid a duplicate `vendor_id` conflict (the stealth tuning uses `GENUINE00000` vs the old `random`),
  - new CLI flags: `--stealth-vm-tuning` (force on, skip prompt), `--no-stealth-vm-tuning` (force skip), and a standalone `--install-stealth-vm-tuning` mode (re-apply/refresh tuning on existing VMs without re-running the full wizard, like `--install-graphics-daemon`); all three are in `parse_args`, usage/help, and fish/bash/zsh completions,
  - fixed an idempotency bug in the ported python: removed the redundant `root.set('xmlns:qemu', QEMU_NS)` that caused a duplicate-attribute `ParseError` on re-parse (the `ET.register_namespace` alone is sufficient for the serializer to emit `xmlns:qemu`); re-running on an already-tuned VM no longer crashes (structural features are stable; only the randomized SMBIOS serial/UUID regenerate, matching the original Stealthy-VM behavior),
  - functional smoke verified: a mock VM XML through the python tuning adds all 8 features (vendor_id=GENUINE00000, kvm hidden, hypervisor CPUID disable, -cpu kvm=off, -smbios manufacturer=ASUS, e1000e NIC, Samsung_ disk serial, memballoon=none) and removes the tablet input; `bash -n` + `shellcheck` clean on `vfio.sh`; embedded python compiles (`py_compile`); full `regression/script.sh` suite = 17 PASS / 0 FAIL,
  - added Q3v regression block (~30 assertions) to `regression/dynamic-binding-regression.sh`: function defined, CLI flags in parse_args/usage/completions (fish/bash/zsh), dynamic install calls `install_stealth_vm_tuning` (not `install_hypervisor_hiding`), verify-before-define (`virt-xml-validate` + `prompt_yn`), skips running VMs, python exit-3 handled, MIT license attribution, standalone MODE dispatch; updated the stale Q3u assertions (bash completion opts string, dynamic note step 8) for the new stealth tuning.
- 2026-08-10 13:40 UTC: Corrected the full-wizard dynamic-binding install path in `vfio.sh` (the earlier 13:34 change over-skipped optional prompts that are NOT early-binding-specific). The corrected behavior:
  - when the user chooses dynamic binding AND the guest GPU is AMD, the three bootloader functions (`systemd_boot_add_kernel_params` openSUSE `/etc/kernel/cmdline` block, `grub_add_kernel_params` classic-GRUB block, and the generic systemd-boot current-entry block) NON-interactively auto-add `vfio-pci.disable_idle_d3=1`, `pcie_port_pm=off`, and `pcie_aspm=off` (the RX 9070 / RDNA4 reset-bug stability set the user's cmdline has); the reset-bug pair honors `--no-amd-disable-idle-d3` / `--no-amd-pcie-port-pm-off` opt-outs,
  - the optional prompts (USB/xHCI, boot framebuffer, the AMD 4-param power-tradeoff prompt, SELinux/AppArmor, ACS override, boot verbosity, boot target, custom kernel params) are NOT early-binding-specific — they apply to both modes — so they STILL RUN in dynamic mode (the earlier 13:34 version wrongly skipped them); a dynamic user still gets asked about ACS / framebuffer / SELinux / etc.,
  - the only truly early-binding-specific skips are `vfio-pci.ids=` and `rd.driver.pre=vfio-pci` (dynamic must NOT pin IDs / pre-load vfio-pci at boot since amdgpu loads first), which already had their own dynamic guards before this change and are unchanged,
  - `apply_configuration()`'s outer "Enable IOMMU kernel parameters now?" gateway prompt still auto-proceeds in dynamic mode (`if [[ DYNAMIC ]] || prompt_yn …`) since IOMMU is mandatory for passthrough in both modes; early binding still asks (default Y),
  - the persistence target is distro-aware: openSUSE writes `/etc/kernel/cmdline` + syncs BLS via `sdbootutil`, classic GRUB edits `/etc/default/grub` + runs `grub-mkconfig`, generic systemd-boot edits the selected entry in-place,
  - verified the openSUSE dynamic path auto-lands exactly `amd_iommu=on iommu=pt vfio-pci.disable_idle_d3=1 pcie_port_pm=off pcie_aspm=off` (the user's current cmdline stability set) without a prompt, then still offers the optional both-mode prompts; `bash -n` + `shellcheck` clean; full `regression/script.sh` suite = 17 PASS / 0 FAIL.
- 2026-08-09 21:49 UTC: README — tightened the "Keeping the RX 9070 alive" subsection to lead with the practical result, additively (existing text kept intact): added a punchy blockquote "practical result" headline as the first thing in the subsection stating the two concrete outcomes (Windows soft reboot works; hard-killing the VM via `virsh destroy` / force-poweroff / BSOD works without the `Unknown PCI header type 127` zombie state) and naming the specific technique that makes the soft reboot reliable (pre-FLR Gen1 PCIe downtrain + adaptive post-FLR link restore, and why — without it the on-card switch fails to retrain the link after an FLR and the card wedges), plus two "Why soft reboot now works" / "Why hard kill now works" paragraphs tying each outcome to its mechanism (reboot-FLR monitor + Gen1-downtrain/adaptive restore for soft reboot; parked-on-vfio-pci + release-time remove+rescan + cooldown probe + alive-check for hard kill), and led the "What's new" intro with the same practical headline.
- 2026-08-09 21:40 UTC: README — surfaced the standout RX 9070 / RDNA4 work that was previously only in the changelog, additively (existing text kept intact):
  - added a prominent new `### Keeping the RX 9070 alive: soft reboot, hard kill, and the zombie card` subsection at the top of the "What's new" section and a matching Quick links entry, plus a one-sentence teaser in the "What's new" intro pointing to it,
  - the subsection strongly highlights the three concrete working mitigations that make the script more than a general VFIO helper: (1) the reboot-FLR monitor that keeps the RX 9070 alive through a guest soft reboot by doing a soft FLR to clear the display wedge without dropping the PCIe link, (2) the Gen1-downtrain + adaptive PCIe link restore technique (pre-FLR force Gen1, post-FLR restore to the hardware's actual max with bounded descent Gen5→Gen1, width logging, GPU alive-check, SKU resolution, VFIO_REBOOT_FLR_MAX_GEN cap), and (3) the three-layer zombie-card recovery for rapid stop/start (release-time remove+rescan, cooldown readiness probe, alive-check + clean abort instead of qemu's `Unknown PCI header type 127` crash),
  - added two Mermaid diagrams: a sequence diagram of the warm-reboot FLR + Gen1-downtrain + adaptive restore flow, and a color-styled flowchart of the stop/start zombie-recovery decision (green = ok, red = abort cleanly, blue = check), matching the existing README diagram style.
- 2026-08-09 21:22 UTC: Hardened the reboot-FLR monitor's RX 9070 family Gen1-downtrain / adaptive link restore in `vfio.sh` with 7 additive improvements (all gated to the RX 9070 family, vendor 1002 / device 7550):
  - SKU resolution: added `_rx9070_sku_name()` which reads the PCI revision from sysfs and maps the RX 9070 / 9070 XT / 9070 GRE (all share 0x7550) to a SKU name for diagnostic logging (observed rev 0xc0 -> 9070 XT, 0xc2 -> 9070 GRE, 0xc3 -> 9070 base; unknown revisions fall back to a labeled "unknown SKU" string); the pre-FLR jlog now includes the SKU plus the detected cap/cur/width, e.g. `pre-FLR Gen1 downtrain on upstream … (RX 9070 XT; cap=Gen5 cur=Gen4 width=x16)`,
  - defensive setpci parsing: added `_setpci_word()` which trims whitespace, validates hex, and left-pads to 4 digits before the `LnkCtl2` nibble slice, so `${_ctl2:3:1}` / `${_ctl2:0:3}` no longer assume a clean 4-char hex string from setpci (the old slice could grab the wrong nibble if setpci emitted leading/trailing whitespace or a short value); all `88.w`/`68.w`/`6A.w` reads in the monitor now go through this helper,
  - bounded descent: replaced the one-step restore fallback (`for _try in 1 2`, which only dropped one gen) with a bounded C-style `for (( _g = _target; _g >= 1; _g-- ))` descent that accepts the highest gen the link actually negotiates at — so a Gen5 target that only retrains at Gen4 will now also try Gen3 if Gen4 also fails, down to Gen1 (guaranteed by the pre-FLR downtrain); bounded (<= 6 iterations) so it can never loop forever; each gen attempt polls `current_link_speed` for up to ~0.6s,
  - `VFIO_REBOOT_FLR_MAX_GEN` operator cap: new conf key (default empty = no cap, 1..6 clamps) in `/etc/vfio-gpu-passthrough.conf` via `write_conf()`, read by the restore to clamp the target for boards that reliably retrain at a lower gen but not the card's full capability (e.g. a Gen5 card in a board that only retrains Gen4 after an FLR) so it skips the failing higher-gen attempt every warm reboot,
  - link width logging: added `_port_link_width()` reading sysfs `current_link_width`; the negotiated/restore-OK/degraded diagnostics now include the width (e.g. `link negotiated at Gen4 x8`), so an x16->x8 degradation from a bad slot/cable is visible alongside the speed (the speed check alone cannot see a width problem),
  - GPU-endpoint alive-check: added `_gpu_alive()` (vendor sysfs not 0xffff + config space not all-ff) run after the restore to catch the case where the LINK is back at a good gen but the GPU itself is still wedged; logs a non-fatal `GPU endpoint not alive after FLR restore … a host reboot may be needed` WARN (does not fail the FLR since the display-wedge clear already succeeded),
  - stronger setpci-missing notice: `install_reboot_flr_monitor()` now checks for `setpci` (pciutils) at install time and prints a clear note naming the distro install command (openSUSE `zypper in pciutils`, Fedora `dnf install pciutils`, Debian `apt-get install pciutils`, Arch `pacman -S pciutils`) when it is absent, so the warm-reboot fix being silently inactive is obvious instead of only logged at FLR time,
  - `do_flr()` resets the new `_FLR_DETECTED_WIDTH` / `_FLR_SKU_NAME` stash at entry alongside the existing stash vars so a previous run can't leak in.
  - updated `regression/dynamic-binding-regression.sh` Q3t (61 assertions) and `regression/dynamic-binding-smoke.sh` Q3t (10 cases): added coverage for `_rx9070_sku_name` (rev c0/c2/c3/c9 -> XT/GRE/base/unknown via mock sysfs revision), `_setpci_word` defensive trim+pad (`  45 ` -> 0045, `5` -> 0005), `_port_link_width` (mock current_link_width=16), `VFIO_REBOOT_FLR_MAX_GEN` clamp (5->4 with cap, 5 with no cap), bounded descent multi-step drop (Gen5 target / Gen3 hw -> 3, which the old one-step fallback could not do), `_gpu_alive` wiring, width in diagnostics, the write_conf default, the installer setpci/pciutils notice, and that the old `_post_flr_restore_gen5` name, `${_saved_hi}5` value, and `for _try in 1 2` one-step loop are all gone.
- 2026-08-09 21:10 UTC: Made the reboot-FLR monitor's pre-FLR Gen1 downtrain / post-FLR link restore hardware-adaptive and documented the RX 9070 family coverage in `vfio.sh`:
  - the `_RX9070_DEVICE_ID` gate comment now documents that the RX 9070, RX 9070 XT, and RX 9070 GRE ALL share PCI device ID `0x7550` (Navi 48 / gfx1201 — same ASIC, differentiated by subsystem/BIOS/revision, not by base device ID), so the existing `7550` gate already covers the whole consumer 9070 family; `0x7551` (Radeon AI PRO R9700, same Navi 48 silicon) is noted as a non-consumer SKU the adaptive restore would handle identically if added,
  - added `_speed_to_gen()` + `_port_speed_gen()` helpers that parse the kernel sysfs `max_link_speed` / `current_link_speed` strings (e.g. "16 GT/s") into a PCIe generation integer (1=Gen1 .. 6=Gen6) so the restore reads the real hardware capability instead of guessing config-space offsets,
  - `_pre_flr_gen1_downtrain()` now detects the upstream switch port's link capability and current negotiated speed (stashed in `_FLR_DETECTED_CAP` / `_FLR_DETECTED_CUR`) and saves the original `LnkCtl2` target nibble (`_FLR_SAVED_TARGET`) / full `LnkCtl2` (`_FLR_SAVED_CTL2`) before forcing Gen1, so the restore can adapt to the actual hardware (Gen4 slot/mobo vs Gen5 card) instead of blindly forcing Gen5,
  - replaced the hardcoded Gen5-only `_post_flr_restore_gen5()` with `_post_flr_restore_target()`, which chooses the restore target in priority order: pre-FLR negotiated speed when below the port's capability (slower slot / signal-integrity / Gen4 board — keeps the proven gen) > detected capability > saved `LnkCtl2` target nibble > Gen5 last-resort default, clamped to a sane Gen1–Gen6 range,
  - the new restore verifies the negotiated speed after retrain and, if the link did NOT reach the target (a hardware/signal-integrity problem), drops one generation and retries once (bounded) so the policy ADAPTS instead of leaving the link degraded or failing the FLR; if it still settles below the detected capability it logs a `possible hardware/signal-integrity issue — link up but degraded` diagnostic for the operator without failing the FLR (the display-wedge clear already succeeded),
  - `do_flr()` resets the detection stash at entry (so a previous run can't leak in) and now calls the adaptive restore; comments updated from "Gen5"/"Gated to RX 9070 ONLY" to reflect the adaptive Gen4/Gen5 restore + RX 9070 family gating,
  - updated `regression/dynamic-binding-regression.sh` Q3t and `regression/dynamic-binding-smoke.sh` Q3t to assert the family comment, the `_speed_to_gen`/`_port_speed_gen` helpers, the `_FLR_DETECTED_CAP`/`_FLR_DETECTED_CUR`/`_FLR_SAVED_TARGET` detection, the adaptive restore target selection (Gen5 slot / Gen4 slot / degraded link / saved-only / none -> Gen5), the degraded-link fallback, the hardware-problem diagnostic, and that the old `_post_flr_restore_gen5` name and hardcoded `${_saved_hi}5` value are gone; smoke test exercises the speed-string -> gen parser and adaptive target picker with a standalone script.
- 2026-08-03 20:42 UTC: Added optional dynamic (libvirt-hook) GPU binding mode and standalone binding-mode switchers in `vfio.sh`:
  - new `VFIO_BINDING_MODE` config key (`early` default | `dynamic`) persisted via `write_conf()`, plus `VFIO_DYNAMIC_REBIND_HOST` (default `0`) to optionally rebind the guest GPU back to the host driver on VM stop,
  - new `--binding-mode early|dynamic` install-mode override flag with `normalize_binding_mode_arg()`, `parse_args` cases, usage/help, and fish/bash/zsh completions,
  - new constants `LIBVIRT_HOOK_DIR`/`LIBVIRT_HOOK_ENTRY`/`LIBVIRT_HOOK_SCRIPT` and `install_libvirt_hook()` that writes a generic `/etc/libvirt/hooks/qemu` entry point plus a standalone `vfio-libvirt-hook.sh` with NO hardcoded VM/domain names,
  - the hook reads the VM domain XML from stdin, extracts PCI `<hostdev>` addresses with an awk parser (`extract_hostdev_bdfs()`), and only switches the guest GPU to vfio-pci (`--bind-now`) when the configured guest GPU BDF is actually attached to that VM; unrelated VMs pass through untouched,
  - generated `vfio-bind-selected-gpu.sh` refactored with `--bind-now`/`--release` arg parsing and a dynamic-mode boot no-op guard so the existing systemd unit stays harmless in dynamic mode,
  - `bind_one()` now sets `d3cold_allowed=0` per-device (RX 9070 / RDNA4 reset-bug fix) and verifies the device actually landed on vfio-pci after bind, returning non-zero on failure so the libvirt hook aborts the VM start cleanly; `do_bind()` propagates the failure for both the guest GPU and audio functions,
  - dynamic mode skips/strips `vfio-pci.ids=` and `rd.driver.pre=vfio-pci` from the kernel cmdline in all 3 bootloader paths (openSUSE `/etc/kernel/cmdline`, systemd-boot entry, GRUB) while keeping IOMMU and AMD stability params,
  - new standalone CLI modes `--install-dynamic-binding` and `--install-early-binding` (mirror `--install-graphics-daemon`) to switch an existing setup between binding modes without re-running the full wizard: flip the conf key, install/remove the hook, strip/re-add early-binding cmdline tokens, sync BLS,
  - new `maybe_offer_detect_dynamic_binding()` detect-mode remediation that offers to switch to dynamic binding when an AMD guest GPU is still on amdgpu with reset-bug markers in kernel logs and the current mode is early,
  - `--verify` no longer fails the guest-GPU-not-on-vfio-pci check in dynamic mode (expected on amdgpu until VM start) and instead checks the hook script + qemu entry presence; `--detect` reports binding mode and libvirt hook status,
  - `--reset` and rollback now remove/restore the libvirt qemu hook (restoring any pre-existing user-managed hook from `.bak` backup),
  - wizard binding-mode prompt now shows a readable early-vs-dynamic comparison column (trade-offs) and defaults to dynamic when an AMD guest GPU is currently on amdgpu,
  - added `regression/dynamic-binding-regression.sh` with static wiring + functional assertions (VM-XML detection, dynamic cmdline stripping, unbind/bind flow, post-bind verification, failure propagation) ending with a FAIL SUMMARY block.
- 2026-08-03 20:42 UTC: Added AMD GPU stability kernel parameters `vfio-pci.disable_idle_d3=1` and `pcie_port_pm=off` in `vfio.sh`:
  - both added to the AMD-gated stability workaround blocks in all 3 bootloader paths (openSUSE `/etc/kernel/cmdline`, systemd-boot entry, GRUB) alongside the existing `amdgpu.runpm=0`/`amdgpu.noretry=0`, with explanatory notes (D3 entry/exit reset races and PCIe port PM link drops),
  - new override flags `--amd-disable-idle-d3`/`--no-amd-disable-idle-d3` and `--amd-pcie-port-pm-off`/`--no-amd-pcie-port-pm-off` (var declarations, `parse_args`, usage/help, fish/bash/zsh completions) extending the 4-flag override pattern,
  - both parameters added to `print_manual_iommu_instructions()` for unsupported bootloaders and removed by both `--reset` cleanup paths (classic GRUB and openSUSE `/etc/kernel/cmdline`),
  - extended `regression/custom-kernel-params-regression.sh` with 28 assertions covering all 3 AMD stability blocks, override parsing, reset removal, usage, and completions.
- 2026-06-05 20:16 UTC: Added USB mitigation reset-counter and `--usb-mitigation-status` reporting mode in `vfio.sh`:
  - new constant `USB_BT_STATE_FILE="/var/lib/vfio-usb-bt-mitigation.state"` for persistent state tracking,
  - generated `vfio-usb-bluetooth.sh` now writes a state entry (`timestamp=<ISO> mode=<disable|enable> targets=<bus-port:vid:pid,...>`) on every disable/enable run,
  - new `usb_bt_mitigation_status()` function aggregates total runs, per-device counts, and last-run timestamp from the state file,
  - kernel log scan (journalctl/dmesg) counts USB reset/timeout/enumeration/disconnect markers and prints a graded summary (`OK`/`WARN`),
  - CLI parser and main dispatch now recognize `--usb-mitigation-status` as a dry-run reporting mode,
  - updated usage/help, fish/bash/zsh completions, and `reset_usb_mitigation_only()` cleanup to include `USB_BT_STATE_FILE`,
  - added `regression/usb-mitigation-status-regression.sh` with coverage for state-file parsing, missing-file handling, CLI dispatch, generated helper format, and reset-cleanup removal.
- 2026-06-05 19:04 UTC: Added Fedora-specific CachyOS kernel prompt for ACS override support in `vfio.sh`:
  - detects Fedora-like systems via `/etc/os-release` `ID`/`ID_LIKE` (`fedora*`, `rhel*`) and `dnf` availability,
  - only triggers for AMD guest GPUs (`1002`) when the guest GPU is not already bound to `vfio-pci`,
  - checks if `pcie_acs_override` is already active in the running kernel or if the CachyOS kernel is already installed/running (`*cachyos*`),
  - explains that the stock Fedora kernel does NOT include the ACS override patch, which can cause IOMMU group sharing and PCIe instability during long VFIO passthrough sessions (USB/xHCI crashes),
  - offers to install `kernel-cachyos` + `kernel-cachyos-devel-matched` from the `bieszczaders/kernel-cachyos` COPR,
  - defaults to `Y` when the guest GPU is currently driven by `amdgpu`, otherwise defaults to `N`,
  - added `fedora_like_detection_reason()`, `is_fedora_like()`, and `maybe_offer_fedora_cachyos_kernel()` helpers,
  - call site placed after `maybe_offer_kernel_longterm()` in the install flow so both openSUSE and Fedora kernel offers can coexist independently.
- 2026-06-03 19:21 UTC: Added CLI override flags for AMD GPU stability workarounds in `vfio.sh`:
  - new flags: `--amd-runpm`, `--no-amd-runpm`, `--amd-noretry`, `--no-amd-noretry`,
  - allow non-interactive install-mode re-runs by forcing or skipping the AMD workaround prompts,
  - overrides are respected in GRUB, openSUSE BLS persistence, and generic systemd-boot paths,
  - updated usage/help, fish/bash/zsh completions, and all three bootloader flow blocks.
- 2026-06-03 19:16 UTC: Added AMD GPU stability workarounds (`amdgpu.runpm=0`, `amdgpu.noretry=0`) to install and reset flows in `vfio.sh`:
  - offered interactively in GRUB, openSUSE BLS persistence, and generic systemd-boot paths when guest GPU vendor is AMD (`1002`),
  - parameters are removed during `--reset` for both classic GRUB and openSUSE `/etc/kernel/cmdline` paths,
  - detailed explanations explain what each parameter does, why it helps, and that these are **workarounds not solutions**,
  - includes trade-off warnings (higher idle power/heat for `amdgpu.runpm=0`, and PCIe retry-loop behavior change for `amdgpu.noretry=0`),
  - added to `print_manual_iommu_instructions()` for unsupported bootloaders,
  - updated regression script assertions where applicable.
- 2026-04-01 20:09 UTC: Clarified USB mitigation hard-block UX messaging in `vfio.sh`:
  - hard-block interactive flow now explicitly warns that active `authorized=0` state blocks matched devices from VM USB passthrough attach,
  - added explicit rationale text that hard-block exists as a last-resort fallback when detach-only mitigation cannot stop unstable USB reset/timeout loops,
  - updated `regression/usb-storage-exclusion-regression.sh` (`case17`) to assert passthrough-impact and rationale messaging remains present.
- 2026-04-01 19:49 UTC: Improved USB mitigation hard-block disable behavior in `vfio.sh`:
  - `configure_usb_bt_hard_block_interactive` now restores `authorized=1` for devices that matched the previously active hard-block policy when hard-block is turned off,
  - prevents stale `authorized=0` state after users disable hard-block, so devices are unblocked immediately without manual sysfs writes,
  - added regression coverage in `regression/usb-storage-exclusion-regression.sh` (`case21`) to validate scoped authorization restore on hard-block disable.
- 2026-03-31 20:12 UTC: Extended USB mitigation effective-target color rendering in `vfio.sh`:
  - summary totals now colorize `mitigate`, `hard-block`, and `eee-off` counters to match their corresponding state-tag colors when `ENABLE_COLOR=1`,
  - plain no-color totals output remains unchanged for `ENABLE_COLOR=0`,
  - updated `regression/usb-storage-exclusion-regression.sh` `case20` assertions to validate colored totals counters in color-enabled output.
- 2026-03-31 20:10 UTC: Added colorized USB mitigation effective-target status rendering in `vfio.sh`:
  - `[MITIGATE]`, `[HOST-BOUND]`, `[HARD-BLOCK]`, and `[EEE-OFF]` tags now render with explicit per-state colors when `ENABLE_COLOR=1` while retaining plain-text fallback when color is disabled,
  - legend output now mirrors those same colored state tags for faster status scanning consistency with the rest of the mitigation flow,
  - extended `regression/usb-storage-exclusion-regression.sh` with `case20` to validate colored legend tags, colored stacked state tags, and unchanged summary totals.
- 2026-03-31 20:00 UTC: Clarified USB mitigation bluetooth.service guard prompt/status messaging in `vfio.sh`:
  - flow now prints explicit current status (`enabled`/`disabled`) before the yes/no prompt,
  - unchanged branch now reports explicit final state (`bluetooth.service stop/start integration remains <enabled|disabled>`) instead of generic `unchanged`,
  - added regression coverage (`case19`) in `regression/usb-storage-exclusion-regression.sh` for unchanged-status clarity output.
- 2026-03-31 19:58 UTC: Corrected USB mitigation effective-target hard-block summary behavior in `vfio.sh`:
  - `[HARD-BLOCK]` tags/counts now apply only to devices that are mitigation targets under the active match policy,
  - prevents misleading `[HOST-BOUND][HARD-BLOCK]` summary lines in include-only targeting scenarios,
  - added regression coverage in `regression/usb-storage-exclusion-regression.sh` to assert host-bound entries never receive hard-block tags and totals stay mitigation-scoped.
- 2026-03-31 19:43 UTC: Fixed Bluetooth hint/index classification gaps in USB mitigation picker and helper matching:
  - `usb_sysfs_device_is_bluetooth()` and generated `vfio-usb-bluetooth.sh` bluetooth detection now include product/manufacturer text fallback (`bluetooth`) when btusb/class metadata is unavailable,
  - restores expected Bluetooth hints and yellow picker index emphasis for adapters that expose incomplete interface metadata during mitigation states,
  - extended `regression/usb-storage-exclusion-regression.sh` with explicit fallback-detection assertions and generated-helper fallback wiring checks.
- 2026-03-31 19:37 UTC: Improved USB mitigation usability and targeting visibility in `vfio.sh`:
  - added interactive configuration for `USB_BT_STOP_BLUETOOTH_SERVICE` so users can explicitly enable/disable bluetooth.service stop/start integration during mitigation runs,
  - added installer output section `USB mitigation effective targets` that enumerates detected USB devices with policy tags (`[MITIGATE]`, `[HOST-BOUND]`, `[HARD-BLOCK]`, `[EEE-OFF]`) and totals,
  - added helper matching utilities used by summary output to mirror active match-policy behavior for include/exclude, hard-block scope, and USB Ethernet EEE scope.
- 2026-03-31 19:14 UTC: Expanded USB Bluetooth mitigation control surface in `vfio.sh`:
  - mitigation match-policy defaults now include additive keys `USB_BT_STOP_BLUETOOTH_SERVICE`, `USB_BT_HARD_BLOCK`, and `USB_BT_HARD_BLOCK_IDS`,
  - installer reruns now backfill missing advanced keys in existing `/etc/vfio-usb-bluetooth-match.conf` files and treat non-default advanced values as preconfigured policy,
  - generated `vfio-usb-bluetooth.sh` now normalizes boolean flags, supports optional bluetooth service stop/start integration, and supports optional USB hard-block (`authorized` 0/1) for selected IDs,
  - install output and mitigation explain text now document the new advanced knobs and conservative defaults.
- 2026-03-28 21:13 UTC: Added additive bootlog removal alias support:
  - `vfio.sh` now accepts `--remove-bootlog` as an alias of `--disable-bootlog`,
  - updated usage/help and fish/bash/zsh completion generators to include the new alias,
  - updated README command-mode docs and Unreleased notes to document the install/remove bootlog pairing (`--install-bootlog` / `--remove-bootlog`).
- 2026-03-28 21:07 UTC: Expanded targeted regression depth for recent USB mitigation changes:
  - `regression/usb-ethernet-eee-regression.sh` now verifies no-color fallback output (`ENABLE_COLOR=0`) keeps plain numbered EEE picker indices while still persisting selected USB Ethernet IDs,
  - `regression/reset-usb-mitigation-regression.sh` now verifies top-level `main --reset-usb-mitigation` parser/dispatch wiring (including root/systemd/writable-root gates) reaches the USB-only reset path and preserves core VFIO artifacts.
- 2026-03-28 21:03 UTC: Improved USB Ethernet EEE-off interactive picker output in `vfio.sh`:
  - detected USB Ethernet option indices now use the same colorized numbering style as other USB mitigation pickers when color output is enabled.
- 2026-03-28 21:03 UTC: Expanded regression coverage for recent USB/reset/completion improvements:
  - added `regression/reset-usb-mitigation-regression.sh` to verify USB-only reset removes USB mitigation artifacts while preserving core VFIO artifacts,
  - added `regression/usb-ethernet-eee-regression.sh` to verify EEE-off picker colorized numbering and persisted selected IDs,
  - strengthened `regression/completion-output-regression.sh` to assert multiline zsh `_arguments` continuation structure.
- 2026-03-28 20:49 UTC: Added USB Ethernet EEE-off integration to USB mitigation flow in `vfio.sh`:
  - mitigation config now persists `USB_ETHERNET_EEE_OFF` and `USB_ETHERNET_EEE_IDS` in `/etc/vfio-usb-bluetooth-match.conf`,
  - installer flow now supports interactive USB Ethernet ID selection for EEE-off policy,
  - generated mitigation helper now applies `ethtool --set-eee ... eee off` only for selected USB Ethernet targets with USB-only safeguards.
- 2026-03-28 20:49 UTC: Added dedicated `--reset-usb-mitigation` mode in `vfio.sh`:
  - removes only USB mitigation helper/unit/udev/match-policy artifacts,
  - resets USB mitigation state including USB Ethernet EEE-off config keys,
  - keeps core VFIO GPU passthrough configuration intact.
- 2026-03-28 20:49 UTC: Updated CLI/help/completion wiring for USB mitigation reset mode:
  - parser and main dispatch now recognize `--reset-usb-mitigation`,
  - usage/help output now documents the USB-only reset behavior,
  - fish/bash/zsh completion generators now include `--reset-usb-mitigation`.
- Improved USB Bluetooth mitigation helper behavior in `vfio.sh` for advanced include-only policy:
  - generated `vfio-usb-bluetooth.sh` now detaches explicitly selected non-Bluetooth USB interfaces when `MATCH_MODE=\"include_only\"` and matching `INCLUDE_IDS` are configured.
  - default Bluetooth-only behavior in `MATCH_MODE=\"auto\"` remains unchanged.
  - mitigation flow now includes an explicit policy-mode prompt (`auto` Bluetooth-only vs `include_only` advanced targeted IDs).
  - helper now enforces USB-only interface/driver path guards so motherboard PCI ethernet/Wi-Fi devices are never targeted by this mitigation path.
  - helper `--enable` path now uses generic USB driver reprobe (`/sys/bus/usb/drivers_probe`) with fallback bind behavior for broader driver compatibility.
- Fixed persisted AUTO X11 pinning default in `vfio.sh`:
  - `write_conf()` now emits `VFIO_GRAPHICS_AUTO_X11_PINNING=\"1\"` as a plain quoted numeric value (not escaped-quote literal text).
  - prevents daemon config parsing from falling back to disabled behavior for AUTO X11 pinning.
- Updated `regression/protocol-mode-regression.sh`:
  - added assertion that `write_conf()` persists `VFIO_GRAPHICS_AUTO_X11_PINNING=\"1\"` correctly.
- Hardened AUTO graphics protocol handoff logic in `vfio.sh` for Wayland logout stability:
  - generated `vfio-graphics-protocold.sh` now keeps X11 host-GPU pinning during active Wayland sessions when the detected display manager is X11-prelogin (`sddm`, `lightdm`, `lxdm`, `xdm`).
  - avoids remove/re-add pinning races at Wayland logout that can cause black-screen before greeter recovery.
- Updated `regression/protocol-mode-regression.sh`:
  - added static assertion for `auto-dm-prefers-x11-prelogin-wayland-session` AUTO decision path wiring.
- Updated graphics daemon polling defaults in `vfio.sh`:
  - `GRAPHICS_DAEMON_INTERVAL_DEFAULT` changed from `2` to `1`.
  - generated `vfio-graphics-protocold.sh` fallback polling default (`DEFAULT_SLEEP_SECS`) changed from `2` to `1`.
- Added `regression/watchdog-detect-defaults-regression.sh`:
  - validates detect JSON watchdog sizing output uses persisted retention/max-line values when valid.
  - validates fallback to default watchdog retention/max-line values when config is missing or out-of-range.
- Added standalone graphics protocol daemon reinstall mode in `vfio.sh`:
  - new `--install-graphics-daemon` mode installs/reinstalls only `vfio-graphics-protocold` + unit using existing persisted config (no full wizard rerun).
- Improved graphics protocol watchdog retention and decision diagnostics in generated `vfio-graphics-protocold.sh`:
  - added retention/line-cap helpers (`watchdog_retention_days`, `watchdog_max_lines`, `prune_watchdog_log`) with defaults of 10 days and 5000 lines.
  - watchdog entries now include structured decision context (`reason`, `dm`, `prelogin`, `host`, `guest`) plus effective retention metadata.
  - transition state keys and daemon stdout transition lines now include reason/display-manager context for easier AUTO-policy debugging.
- Extended detect/report wiring in `vfio.sh` for watchdog config visibility:
  - persisted defaults now include `VFIO_GRAPHICS_WATCHDOG_RETENTION_DAYS=\"10\"` and `VFIO_GRAPHICS_WATCHDOG_MAX_LINES=\"5000\"`.
  - detect JSON now exposes `configured_graphics_watchdog_retention_days` and `configured_graphics_watchdog_max_lines`.
  - human detect output now reports normalized graphics-daemon interval, watchdog retention days, and watchdog max-line cap values.
- Updated `regression/protocol-mode-regression.sh` coverage:
  - added static assertions for `--install-graphics-daemon` parser/dispatch wiring and existing-config reinstall helper path.
  - added assertions for watchdog retention/max config persistence, detect JSON watchdog keys, and richer watchdog/state transition context fields.
- Hardened watchdog-correlated graphics protocol AUTO policy in `vfio.sh` for logout/session-switch stability:
  - generated `vfio-graphics-protocold.sh` now resolves active display-manager type via `/usr/lib/X11/displaymanagers/default-displaymanager` fallback when wrapper units (for example openSUSE `display-manager.service`) hide the concrete DM service name.
  - AUTO policy now keeps X11 host-GPU pinning active during X11 sessions when the detected display manager uses X11 prelogin greeters (for example SDDM/LightDM/LXDM/XDM), reducing black-screen risk during logout/greeter restart handoff.
- Added standalone boot-log dumper reinstall mode in `vfio.sh`:
  - new `--install-bootlog` mode installs/reinstalls only the optional boot-log helper + unit without rerunning full interactive setup.
  - intended for snapshot workflows where `/etc` systemd state may diverge from user-home helper files after rollback.
- Added persistent graphics protocol watchdog logging in `vfio.sh`:
  - generated `vfio-graphics-protocold.sh` now writes mode/session/action transitions to a `/home`-hosted watchdog log (`~/.local/state/vfio-graphics-protocol/watchdog.log` when a desktop user is resolved).
  - watchdog lines include timestamp, mode, session type, applied action, and detected root subvolume for snapshot-context debugging.
  - daemon now normalizes watchdog log ownership/perms back to the desktop user so logs remain user-manageable.
- Hardened boot-log dumper ownership behavior in `vfio.sh`:
  - generated `vfio-dump-boot-log.sh` now templates desktop user/group metadata and normalizes `~/Desktop/vfio-boot-logs` ownership/perms on each run.
  - resulting `vfio-boot-*.log` files remain user-manageable (read/delete without sudo) even though capture runs from a root systemd service.
- Hardened cmdline token parsing in `vfio.sh` against inherited non-default shell `IFS`:
  - `cmdline_get_key_value_token`, `cmdline_contains_exact_token`, `cmdline_remove_key_value_tokens`, and related cmdline token-removal helpers now force canonical whitespace splitting internally.
  - prevents false `root=` detection failures in openSUSE `/etc/kernel/cmdline` persistence and `sync_bls_entries_from_kernel_cmdline()` flows when shell word-splitting state is non-standard.
- Updated `regression/custom-kernel-params-regression.sh`:
  - added a functional BLS-sync scenario that forces non-space global `IFS` and verifies root metadata preservation plus baseline VFIO/IOMMU option propagation.
- Added read-only BLS token trace mode in `vfio.sh`:
  - new `--debug-cmdline-tokens` mode now traces baseline and per-entry `root`/`rootflags` source selection used by `sync_bls_entries_from_kernel_cmdline()`.
  - mode forces dry-run behavior so token-source diagnostics can run without mutating Boot Loader Spec entry files.
- Extended BLS token trace mode in `vfio.sh`:
  - added optional `--entry` basename glob filtering for `--debug-cmdline-tokens` runs so tracing can be scoped to matching BLS entry files.
  - added parse-time validation that rejects empty/whitespace-only `--entry` patterns (`--entry=` / `--entry ''` / `--entry '   '`) with an explicit non-empty pattern error.
  - added machine-readable output for `--debug-cmdline-tokens --json` with `mode`, `entry_filter`, `exit_code`, and ordered `lines`.
- Hardened install-time X11 prelogin safety flow in `vfio.sh`:
  - `apply_configuration()` now installs a prelogin host-GPU Xorg pinning failsafe for explicit `X11` mode and `AUTO` mode when using X11-greeter display managers (`sddm`, `lightdm`, `lxdm`, `xdm`).
  - reduces `no screens found`/black-screen failures when the guest GPU is pre-bound to `vfio-pci` before display-manager startup.
- Updated `regression/custom-kernel-params-regression.sh`:
  - added functional coverage that validates `--debug-cmdline-tokens` mode exits cleanly, emits representative baseline/per-entry debug source lines, and keeps BLS entry options unchanged.
  - added functional coverage for `--debug-cmdline-tokens --json` with `--entry` filtering and assertions that non-matching entry traces are excluded.
  - added functional parser coverage for empty/whitespace-only `--entry` rejection in both `--entry=` and split-argument forms.
  - added static wiring assertions for parser and main-dispatch integration of `--debug-cmdline-tokens`.
- Updated `regression/protocol-mode-regression.sh`:
  - added static coverage that guards install-time prelogin X11 host-GPU failsafe helper presence and `apply_configuration()` wiring/order relative to graphics-daemon installation.
- Hardened openSUSE root-metadata safety fallback in `vfio.sh`:
  - openSUSE `/etc/kernel/cmdline` persistence path now attempts recovery from running `/proc/cmdline` when `root=` metadata is still missing after existing cmdline/BLS/current-mount fallback stages.
  - `sync_bls_entries_from_kernel_cmdline()` now also attempts `/proc/cmdline` metadata recovery before emitting root-missing skip behavior.
  - updated skip diagnostics now explicitly include `/proc/cmdline` among attempted metadata sources.
- Updated `regression/custom-kernel-params-regression.sh`:
  - added assertions that guard `/proc/cmdline` metadata fallback wiring for both persistence and BLS synchronization paths.
  - added a functional BLS-sync scenario that simulates missing root metadata in persisted/current-mount/BLS sources and verifies successful recovery from `/proc/cmdline`.
  - added a functional BLS-sync scenario that verifies sync still succeeds when global root metadata is unavailable but per-entry root metadata is present.
- Updated `sync_bls_entries_from_kernel_cmdline()` safety flow:
  - removed global hard-abort behavior when baseline `root=` metadata cannot be recovered.
  - sync now continues with per-entry root preservation and skips only entries that still lack provable root metadata after all fallback stages.
- Hardened openSUSE bootloader detection in `vfio.sh` for GRUB snapshot/BLS layouts:
  - `detect_bootloader()` now classifies openSUSE GRUB systems as `grub2-bls` when `/etc/kernel/cmdline` and Boot Loader Spec entries with `options ... root=` are present, even if `LOADER_TYPE=grub2-bls` / `GRUB_ENABLE_BLSCFG=true` markers are missing.
  - prevents cmdline/VFIO parameter updates from silently falling back to legacy `/etc/default/grub`-only edits while active BLS boot entries remain unchanged.
- Updated `regression/custom-kernel-params-regression.sh`:
  - added explicit assertions that guard the new openSUSE GRUB+BLS fallback detection wiring in `detect_bootloader()`.
- Hardened openSUSE cmdline persistence/root metadata handling in `vfio.sh`:
  - `systemd_boot_add_kernel_params()` now re-applies existing `/etc/kernel/cmdline` boot metadata (`root=`, `rootflags=`, `rootfstype=`, `resume=`, `systemd.machine_id`) before final safety checks so additive VFIO/IOMMU updates do not drop root tokens.
  - persistence path now also uses current-mount metadata fallback (`bls_current_mount_root_token`, `bls_current_mount_rootflags_token`) before triggering the root-token safety abort.
  - `sync_bls_entries_from_kernel_cmdline()` now attempts the same current-mount root/rootflags fallback before skipping BLS sync due missing `root=`.
- Updated `regression/custom-kernel-params-regression.sh` coverage:
  - renamed sdbootutil fallback fixture entry names to non-fallback filenames so fallback-sync assertions validate the intended sync path,
  - added static coverage checks for the new root/rootflags-preserving persistence and current-mount fallback wiring.
- Fixed a prelogin graphics-adaptation crash in generated daemon policy logic:
  - removed an invalid `: "$host_gpu_bdf" "$guest_gpu_bdf"` no-op reference from daemon `apply_policy_once()` generation that could trigger `set -u` unbound-variable failure at boot.
- Extended `regression/protocol-mode-regression.sh` with a guard for that crash path:
  - now asserts generated source no longer contains the undefined host/guest no-op reference pattern.
- Extended `regression/protocol-mode-regression.sh` deferred-activation coverage:
  - validates installer protocol-mode application remains deferred-only (no live X11/Wayland side effects during install),
  - validates graphics protocol daemon install wiring uses `systemctl enable` without `--now`,
  - validates `apply_selected_graphics_protocol_mode "$host_gpu" "$guest_gpu"` remains ordered after host-audio prompt flow in `apply_configuration()`.
- Deferred installer-time graphics protocol activation in `vfio.sh`:
  - `apply_selected_graphics_protocol_mode()` now records/summarizes selected mode behavior without performing live X11/Wayland switching during the install run.
  - protocol adaptation is explicitly deferred to next boot.
- Updated graphics protocol daemon installation behavior in `vfio.sh`:
  - replaced `systemctl enable --now vfio-graphics-protocold.service` with `systemctl enable vfio-graphics-protocold.service`,
  - daemon activation now begins on next reboot instead of immediately during wizard execution.
- Reordered install-flow protocol-mode application messaging:
  - `apply_selected_graphics_protocol_mode` call now runs at the end of `apply_configuration()` after interactive input sections complete.
- Added a top-level README banner image link using a relative path (`icon/images.webp`) to keep documentation image references non-hardcoded and repo-portable.
- Expanded the top-level README banner image rendering to full-width display for improved visual visibility while keeping the same relative link path.
- Improved `regression/custom-kernel-params-regression.sh` CI triage readability:
  - failed assertion names are now collected and printed in a final `FAIL SUMMARY (N)` block before non-zero exit.
- Extended the same CI triage fail-summary pattern across existing regression suites:
  - `regression/boot-vga-policy-regression.sh`
  - `regression/completion-output-regression.sh`
  - `regression/openbox-monitor-regression.sh`
  - `regression/protocol-mode-regression.sh`
  - `regression/usb-storage-exclusion-regression.sh`
- Updated `regression/regression-template.sh` so newly generated regression files inherit the same `FAIL SUMMARY (N)` behavior by default.
- Hardened CI shell-lint compatibility across older ShellCheck releases:
  - updated modified scripts to explicitly consume intentionally ignored capture variables,
  - avoids legacy `SC2034` false-positive failures in stricter/older ShellCheck environments.
- Hardened `reset_vfio_all()` full-reset behavior in `vfio.sh`:
  - now always removes `vfio-set-host-audio.service` user units for all `/home/*` users (no optional prompt-gating),
  - now always removes VFIO-related boot parameters from `/etc/default/grub` (classic GRUB) and `/etc/kernel/cmdline` (openSUSE/BLS) when those files are present.
- Added no-install shell completion support in `vfio.sh`:
  - new output modes: `--print-fish-completion`, `--print-bash-completion`, `--print-zsh-completion`,
  - completion output now includes all current script options plus `--boot-vga-policy` value completion (`auto`, `strict`).
- Added main-flow handling so completion modes execute before runtime dependency checks, allowing completion generation in minimal environments.
- Added `regression/completion-output-regression.sh`:
  - validates `--print-fish-completion`, `--print-bash-completion`, and `--print-zsh-completion` all produce non-empty output,
  - checks each completion output contains representative option coverage (including `--usb-health-check`, `--print-*-completion`, and `--boot-vga-policy` value completions).
- Extended `regression/completion-output-regression.sh`:
  - now verifies `--help` output includes `--print-fish-completion`, `--print-bash-completion`, and `--print-zsh-completion` so help/completion flags stay aligned.
- Improved `regression/completion-output-regression.sh` option coverage checks:
  - now derives long-option expectations dynamically from `vfio.sh --help`,
  - verifies fish/bash/zsh completion outputs cover that parsed help-option set, reducing maintenance when new flags are added.
- Improved USB Bluetooth mitigation exclusion UX:
  - `configure_usb_bt_exclude_ids_interactive()` now colorizes `[hint: Bluetooth detected]` labels when color output is enabled while keeping plain-text fallback behavior unchanged.
- Extended USB Bluetooth mitigation exclusion hints:
  - picker now marks detected Ethernet and Printer USB devices with distinct colored keep-bound hints to reduce accidental unbind selections.
  - picker guidance now explicitly clarifies that exclusion selections keep devices bound on the host (`EXCLUDE_IDS`).
- Added USB storage danger classification in exclusion picker:
  - picker now detects storage-class USB devices and marks them with a red `[danger: Storage]` label.
  - guidance now explicitly warns that unbinding these can disconnect active host storage and recommends excluding all storage-marked entries.
- Improved exclusion prompt clarity:
  - selection prompt now explicitly says numbers are excluded from unbind (`EXCLUDE from unbind`).
- Added storage omission confirmation interlock in exclusion picker:
  - when any storage-marked USB IDs are not selected for `EXCLUDE_IDS`, picker now shows an explicit danger summary,
  - user must either re-enter exclusions or type `I ACCEPT STORAGE RISK` to continue.
- Improved USB exclusion picker copy for clearer host/VM behavior:
  - prompt/instructions now consistently use natural-language “keep host-bound” wording for selections,
  - guidance now explicitly states Bluetooth intent: selected adapters stay on host, unselected adapters remain VM-detach-eligible.
- Refined storage interlock applicability for USB exclusion policy modes:
  - strict storage risk-confirmation flow now runs only when non-Bluetooth detach is policy-eligible (`MATCH_MODE=include_only` or non-empty `INCLUDE_IDS`),
  - default Bluetooth-only policy (`MATCH_MODE=auto` with empty `INCLUDE_IDS`) now emits informational storage notes without forcing risk-phrase confirmation.
- Reworked USB exclusion picker selection model for clearer passthrough UX:
  - picker input now selects `VM-ELIGIBLE` detach targets directly instead of selecting host-bound exclusions first,
  - `EXCLUDE_IDS` persistence is now derived as the inverse (unselected host-bound IDs) so config behavior remains compatible.
- Added Bluetooth-focused display mode with explicit full-list fallback in USB picker:
  - in default Bluetooth-only policy, picker can show a reduced Bluetooth-focused list to simplify VM-eligible selection,
  - picker now prompts to show the full USB list so users can always switch back to complete device visibility before applying.
- Added in-loop picker view switching commands:
  - users can now type `full` or `focus` while entering VM-eligible selections to switch list views without restarting the picker flow.
- Improved picker retry guidance for view switching:
  - after users decline apply in toggle-capable mode, picker now prints a quick `full` / `focus` reminder before the next entry round.
- Hardened USB Bluetooth exclusion persistence against missing match config:
  - picker now auto-recreates `/etc/vfio-usb-bluetooth-match.conf` with defaults when missing at selection/apply time, preventing awk \"cannot open file\" failures.
- Added idempotent USB Bluetooth exclusion write behavior:
  - `configure_usb_bt_exclude_ids_interactive()` now compares derived exclusions against existing `EXCLUDE_IDS` and skips config writes when unchanged.
- Added idempotent mitigation apply behavior for existing USB Bluetooth service installs:
  - installer now avoids immediate `systemctl enable --now` reruns when exclusion selection is unchanged and the service unit already exists, while still keeping the service enabled.
- Added content-aware artifact write behavior for USB Bluetooth mitigation installer:
  - helper script, systemd unit, and udev rule writes are now skipped when generated content is unchanged on rerun.
- Refined unchanged-rerun immediate-start gating:
  - immediate service start is now skipped only when both exclusion selection and generated artifact content are unchanged for an existing install.
- Added preconfigured USB exclusions/policy detection in mitigation installer reruns:
  - when existing non-default USB Bluetooth policy is detected, installer now asks whether to reconfigure instead of always opening the exclusions picker.
- Added explicit keep-existing policy path for reruns:
  - declining reconfigure now preserves existing exclusions/policy and skips picker execution.
- Updated VM-eligible picker indexing/review behavior for focused view:
  - selection numbers are now mapped to the currently displayed list (focused or full),
  - selection review now uses the same active view indexing so confirmation output matches chosen view mode.
- Updated storage interlock wording/trigger to align with VM-eligible-first flow:
  - interlock now triggers when storage IDs are selected as VM-eligible detach targets,
  - rejection path now loops with VM-eligible wording (`please choose VM-eligible devices again`) for clearer correction flow.
- Added `regression/usb-storage-exclusion-regression.sh`:
  - validates storage-risk exclusion interlock behavior for re-entry, accepted risk phrase, and rejected risk-phrase loop flows.
- Extended `regression/usb-storage-exclusion-regression.sh`:
  - added unchanged-selection idempotency coverage that asserts `EXCLUDE_IDS` write-skip behavior and `USB_BT_EXCLUDE_CHANGED=0`,
  - added installer helper-template generation coverage that bash-syntax-checks generated `vfio-usb-bluetooth.sh`.
- Extended `regression/usb-storage-exclusion-regression.sh` installer rerun coverage:
  - validates unchanged reruns do not rewrite generated helper/unit/udev artifacts,
  - validates second unchanged rerun keeps service enabled without issuing immediate `enable --now`.
- Extended `regression/usb-storage-exclusion-regression.sh` preconfigured-rerun coverage:
  - validates installer rerun detects preconfigured policy and uses explicit reconfigure prompt,
  - validates declining reconfigure skips picker execution while retaining no-immediate-restart behavior.
- Added deterministic test hooks for USB exclusion picker regression control:
  - `VFIO_USB_SYSFS_GLOB` to override USB device discovery source in tests,
  - `VFIO_INTERACTIVE_IN` / `VFIO_INTERACTIVE_OUT` to override picker input/output streams in tests.
- Improved USB exclusion picker readability with category-colored index labels:
  - when color output is enabled, entry numbers (`[n]`) now inherit the strongest matched category color (`Storage > Bluetooth > Ethernet > Printer`) so selection numbers visually match hint labels.
- Improved USB exclusion picker prompt guidance:
  - picker overview now includes explicit multi-select examples (`4 5 6` and `4,5,6`) for excluding many entries in one input.
- Added final selection review/apply loop in USB exclusion picker:
  - picker now always shows a pre-save review of selected exclusions,
  - users can decline apply and re-enter selections before writing `EXCLUDE_IDS`.
- Clarified USB exclusion semantics for VM passthrough workflows:
  - excluded IDs remain host-bound (`EXCLUDE_IDS`),
  - non-excluded IDs remain eligible for automatic detach when using USB passthrough.
- Improved USB exclusion review clarity with per-device mode labels:
  - review now prints a mode summary for each listed USB entry using `HOST-BOUND` and `VM-ELIGIBLE` tags before apply confirmation.
- Improved optional USB Bluetooth mitigation install flow:
  - added interactive numbered USB device listing for `EXCLUDE_IDS` selection,
  - added helper hints that label entries where Bluetooth interfaces are detected.
- Added managed USB Bluetooth match-policy config lifecycle updates:
  - installer now creates `/etc/vfio-usb-bluetooth-match.conf` with `MATCH_MODE`, `INCLUDE_IDS`, `EXCLUDE_IDS`,
  - rollback/reset cleanup now also tracks/removes this match-policy config file.
- Improved USB Bluetooth mitigation trigger and observability:
  - udev rules now include `ACTION=="bind"` for `btusb` interface binds in addition to existing add triggers,
  - install output now reports the installed match-policy config path.
- Improved `--usb-health-check` log-access behavior:
  - now prints explicit sudo guidance for full kernel-log visibility,
  - now falls back to `/var/log/kern.log(.1)` and `/var/log/syslog(.1)` when journal/dmesg data is unavailable.
- Added new read-only mode `--print-effective-config`:
  - reads `/etc/vfio-gpu-passthrough.conf` plus current `boot_vga` sysfs topology,
  - reports normalized `VFIO_BOOT_VGA_POLICY`, effective bind decision (`ALLOW_BIND` / `SKIP_BIND`), and decision reason path (`auto_detect`, `explicit_opt_in`, etc.),
  - runs without applying system changes and is treated as a dry-run/read-only mode.
- Extended `regression/boot-vga-policy-regression.sh` with `print_effective_config()` decision-path checks:
  - verifies strict mode without host-assisted opt-in reports `SKIP_BIND` and `host_assisted_available_but_not_enabled`,
  - verifies `AUTO` policy reports `ALLOW_BIND` with `auto_detect`,
  - verifies strict mode with explicit host-assisted opt-in reports `ALLOW_BIND` with `explicit_opt_in`.
- Added Boot-VGA host-assisted auto-detect/self-adjust support:
  - generated config now persists `VFIO_BOOT_VGA_POLICY="AUTO"` by default,
  - generated bind helper now dynamically adjusts host-assisted Boot-VGA binding each boot based on runtime `boot_vga` topology.
- Improved Boot-VGA health diagnostics for policy mode:
  - strict host-assisted warning is now emitted only when `VFIO_BOOT_VGA_POLICY` is not `AUTO`,
  - warning guidance now includes the new `AUTO` policy path.
- Added install CLI override for Boot-VGA policy:
  - new `--boot-vga-policy auto|strict` argument now controls persisted `VFIO_BOOT_VGA_POLICY` during install runs.
  - install summary now reports the effective Boot-VGA policy and explicitly notes when a CLI override is applied.
- Extended `regression/boot-vga-policy-regression.sh` coverage:
  - verifies `write_conf()` persists `VFIO_BOOT_VGA_POLICY="AUTO"`,
  - verifies `vfio_config_health()` suppresses strict-warning paths when `VFIO_BOOT_VGA_POLICY=AUTO`.
  - verifies `normalize_boot_vga_policy_arg` accepts valid values and rejects invalid policy input.
- Improved `regression/script.sh` shellcheck behavior:
  - when `shellcheck` is missing, the runner now attempts distro-aware installation with package-manager detection (`apt-get`, `dnf`, `yum`, `zypper`, `pacman`, `apk`, `xbps-install`) and sudo/root fallback.
  - when installation still fails, the runner now emits explicit manual-install guidance before continuing remaining checks.
- Cleaned residual shellcheck warnings in regression scripts used by the master runner so shellcheck-enabled runs complete cleanly for protocol/custom-kernel regression paths.
- Added a shared shellcheck suppression convention in override-heavy regression scripts:
  - `SC2317,SC2329` is now declared once at file scope with a convention note,
  - repetitive per-function suppression comments were removed to keep regression scripts easier to maintain.
- Added a reusable regression scaffold at `regression/regression-template.sh`:
  - captures the standardized file bootstrap and assertion helpers,
  - includes the shared file-level `SC2317,SC2329` convention for indirect helper overrides.
- Added helper command `regression/new-regression.sh`:
  - generates `regression/<name>-regression.sh` from the shared template,
  - supports `--force` for explicit overwrite flows,
  - regression auto-discovery now excludes this helper from executable `*-regression.sh` test runs.
- Removed obsolete disabled legacy Xorg helper definitions (`_legacy_*_disabled`) from `vfio.sh` to keep only the active canonical helper implementation path.
- Extended `--detect --json` output with persisted protocol config visibility:
  - added `configured_graphics_protocol_mode` derived from `/etc/vfio-gpu-passthrough.conf` with normalization fallback to `AUTO`.
- Added install-flow protocol-mode selection/wiring updates:
  - install flow now calls `select_and_prepare_graphics_protocol_mode` after graphics preflight,
  - selected mode is now applied via `apply_selected_graphics_protocol_mode` instead of unconditional Xorg prompting.
- Added persisted graphics protocol mode in generated config:
  - `write_conf()` now writes `GRAPHICS_PROTOCOL_MODE` (`X11` / `WAYLAND` / `AUTO` fallback) to `/etc/vfio-gpu-passthrough.conf`.
- Improved mode-specific display behavior:
  - `X11` mode retains explicit Xorg/LightDM host-GPU pinning prompt flow,
  - `WAYLAND` mode skips Xorg forcing and supports optional removal of existing Xorg/LightDM pinning files.
- Improved graphics stack detection:
  - X11 detection now also treats `startx`/`xinit` presence as valid session-path readiness,
  - Wayland detection now treats detected compositor runtime commands as session readiness signals.
- Added `regression/protocol-mode-regression.sh` with coverage for protocol-mode persistence, runtime package mapping, and mode-application call paths.
- Added explicit Xorg-aware install prompting for host/guest GPU split setups:
  - new prompt now reports detected Xorg status (`WORKS`/`NOT_WORK`/`NOT_PRESENT`) before offering host-GPU pinning.
  - prompt text explicitly states that it is an Xorg detection-driven action.
- Added additive host-display safety installers for Xorg/LightDM when accepted in the new prompt:
  - `/etc/X11/xorg.conf.d/20-vfio-host-gpu.conf` to pin Xorg to the selected host GPU BusID.
  - `/etc/lightdm/lightdm.conf.d/90-vfio-host-gpu.conf` to apply `xserver-command ... -isolateDevice` for LightDM.
- Updated `--reset` cleanup to also remove the new Xorg/LightDM host-GPU pinning files.
- Added optional custom kernel-parameter prompts during install boot-option flows for:
  - GRUB kernel cmdline updates,
  - systemd-boot current-entry updates,
  - openSUSE `/etc/kernel/cmdline` persistence updates.
- Prompt guidance now explicitly mentions X11 passthrough-tuning use cases for custom parameters.
- Custom parameters are appended additively as space-separated tokens with existing-option deduplication.
- Hardened `add_custom_kernel_params_interactive()` so command-substitution callers receive only the final cmdline on stdout while prompt/UI text is redirected away from stdout.
- Added optional preview-before-apply confirmation for proposed kernel cmdline updates across install boot-option paths:
  - GRUB cmdline updates,
  - systemd-boot current-entry updates,
  - openSUSE `/etc/kernel/cmdline` persistence updates.
- Expanded splash/verbosity cleanup behavior to include `rhgb`:
  - verbose boot enable flows now remove `rhgb` alongside `quiet`/`splash*`,
  - `--reset` boot-option cleanup now removes `rhgb` from both GRUB and openSUSE `/etc/kernel/cmdline` persistence paths.
- Added `regression/custom-kernel-params-regression.sh` with additive coverage for:
  - dedup token behavior via `add_param_once`,
  - no-change behavior when custom-parameter prompt is declined,
  - helper UI redirection checks guarding against stdout contamination,
  - GRUB/systemd-boot/openSUSE persistence call-site wiring,
  - preview-helper behavior and preview call-site wiring for all boot-option update paths,
  - `rhgb` reset-cleanup wiring checks.
- Added GitHub Actions CI workflow `.github/workflows/regression.yml` to run `regression/script.sh` automatically on both `push` and `pull_request`.
- Improved `regression/script.sh` to auto-discover all `regression/*-regression.sh` scripts, run syntax checks for discovered regression scripts, and execute each regression script automatically in sorted order.
- Improved `regression/script.sh` master-entrypoint coverage:
  - recursive auto-`chmod +x` for `regression/**/*.sh`,
  - recursive bash syntax checks for regression shell scripts,
  - keeps a single-run workflow where only the master runner is needed.
- Restored strict Boot-VGA bind safety in generated `vfio-bind-selected-gpu.sh`:
  - host-assisted Boot-VGA binding now requires explicit opt-in via `VFIO_ALLOW_BOOT_VGA_IF_HOST_GPU=1`,
  - default behavior skips Boot-VGA guest binding unless explicitly forced (`VFIO_ALLOW_BOOT_VGA=1`) or host-assisted opt-in is enabled.
- Improved Boot-VGA host-assisted automation in `vfio.sh` config generation:
  - `write_conf()` now auto-sets `VFIO_ALLOW_BOOT_VGA_IF_HOST_GPU` when topology indicates safe host-assisted binding (`guest boot_vga=1`, different `host boot_vga=0`).
  - this removes the previous manual config step for common dual-GPU host-assisted layouts.
- Improved runtime health diagnostics for Boot-VGA mismatch states:
  - `vfio_config_health()` now warns explicitly when host-assisted Boot-VGA conditions are present but `VFIO_ALLOW_BOOT_VGA_IF_HOST_GPU` is still disabled.
- Added Openbox monitor auto-activation integration in `vfio.sh`:
  - new helper installer for `/usr/local/bin/vfio-openbox-activate-monitors.sh`,
  - additive marker-based hook management for `/etc/xdg/openbox/autostart`,
  - monitor activation logic enables all currently connected outputs via `xrandr --output <name> --auto`.
- Added reset and rollback cleanup coverage for Openbox monitor integration:
  - `--reset` now removes the Openbox helper script and strips the managed Openbox autostart block,
  - generated rollback scripts now restore Openbox files from backup when present, otherwise remove only the managed marker block.
- Added new additive regression assets under `regression/`:
  - `regression/script.sh` unified runner (`bash -n`, optional `shellcheck`, regression execution, and auto-`chmod +x` for discovered scripts),
  - `regression/openbox-monitor-regression.sh` checks parser output, monitor activation call behavior, and idempotent additive autostart hook management.
- Added `regression/boot-vga-policy-regression.sh` to cover Boot-VGA host-assisted policy behavior:
  - verifies `write_conf()` persists computed `VFIO_ALLOW_BOOT_VGA_IF_HOST_GPU` defaults,
  - verifies `vfio_config_health()` emits the Boot-VGA host-assisted warning only when config is misaligned.
- Added Openbox parser smoke coverage to `--self-test`.
- Added `--boot-remove` as an additive alias for `--disable-bootlog` to remove the optional VFIO boot-log dumper.
- Added machine-readable `--detect --json` mode:
  - emits JSON only (no interactive detect remediation prompts),
  - includes tri-state values for `display_manager_health`, `graphics_stack_xorg`, and `graphics_stack_wayland`,
  - tri-state values are stable: `WORKS`, `NOT_WORK`, `NOT_PRESENT`.
- Added display-manager-aware dependency preflight in install flow:
  - `lightdm`: keeps existing AccountsService + fallback handling.
  - `sddm`, `gdm`, `lxdm`, `xdm`: treated as supported without LightDM-specific fallback prompts.
  - unknown/none: explicitly reported and skipped safely.
- Extended `--detect` report with graphics stack tri-state output:
  - `Graphics stack (Xorg)`: `WORKS` / `NOT WORK` / `NOT PRESENT`
  - `Graphics stack (Wayland)`: `WORKS` / `NOT WORK` / `NOT PRESENT`
  - statuses are color-coded (green/red/yellow) when color output is enabled.
- Added explicit graphics protocol support gate for X11/Wayland in install flow:
  - new `graphics_protocol_preflight` now requires at least one of X11(Xorg) or Wayland to be `WORKS` before installation continues.
- Extended `--detect`/`--verify` reporting with protocol-level fields:
  - `Graphics protocol support`: `WORKS` / `NOT WORK` / `NOT PRESENT`
  - `Graphics protocol mode`: `BOTH` / `X11` / `WAYLAND` / `NONE` / `PARTIAL`
  - `--detect --json` now emits `graphics_protocol_support` and `graphics_protocol_mode`.
- Extended `--detect` report with display-manager readiness lines:
  - `Display manager` (detected DM or not present)
  - `Display manager health` (`WORKS` / `NOT WORK` / `NOT PRESENT`)
  - includes explicit LightDM + AccountsService missing context when applicable.
- Added Openbox-aware status reporting to `--detect`/`--verify`:
  - new `Window manager (Openbox)` tri-state line (`WORKS` / `NOT WORK` / `NOT PRESENT`).
  - `--detect --json` now emits `window_manager_openbox` using the same stable tri-state values.
- Added additive window-manager coverage in `--detect`/`--verify`:
  - new tri-state lines for `Window manager (i3)`, `Window manager (bspwm)`, and `Window manager (awesome)`.
  - `--detect --json` now emits `window_manager_i3`, `window_manager_bspwm`, and `window_manager_awesome` with stable tri-state values.
- Added further additive window-manager coverage in `--detect`/`--verify`:
  - new tri-state lines for `Window manager (dwm)`, `Window manager (qtile)`, and `Window manager (xfwm4)`.
  - `--detect --json` now emits `window_manager_dwm`, `window_manager_qtile`, and `window_manager_xfwm4` with stable tri-state values.
- Added a Boot-VGA safety guard to the generated `/usr/local/sbin/vfio-bind-selected-gpu.sh`:
  - if the selected guest GPU is currently marked `boot_vga=1`, the script now skips binding by default to prevent host display-manager lockups.
  - explicit override is available with `VFIO_ALLOW_BOOT_VGA=1` for intentional headless/advanced workflows.
- Tightened Boot-VGA override behavior after black-screen regressions:
  - host-GPU-assisted Boot-VGA binding now requires explicit opt-in via `VFIO_ALLOW_BOOT_VGA_IF_HOST_GPU=1`.
  - default behavior remains safe skip for Boot-VGA guest devices unless explicitly overridden.
- Updated boot-parameter generation to avoid forcing `vfio-pci.ids=...` when the selected guest GPU is currently Boot VGA on the host.
- This reduces LightDM/Xorg startup failures caused by early vfio binding of the active host display adapter.
- Updated Boot-VGA kernel-parameter handling in install flows to add selected guest `vfio-pci.ids=...` by default and use an automatic fallback path:
  - when Boot-VGA is detected and VFIO risk/failure markers are present in the same run, the script now removes that `vfio-pci.ids=...` token from the proposed cmdline before write.
  - this keeps behavior additive-first while preserving safer fallback when failure signals are detected.
- Added shared temporary-directory cleanup helper `cleanup_tmp_dir_best_effort()` and refactored source-based `vendor-reset` DKMS install cleanup branches to use it consistently.
- Extended `regression/custom-kernel-params-regression.sh` with runtime Boot-VGA fallback behavior coverage for `append_guest_vfio_ids_with_detect_fallback()`:
  - validates add-first append behavior when risk markers are absent,
  - validates risk-triggered fallback removal of `vfio-pci.ids=...` and `CTX[guest_vfio_ids_fallback]=1` marker updates.
- Added deterministic openSUSE Boot Loader Spec option synchronization for `grub2-bls` and `systemd-boot`:
  - new helper `sync_bls_entries_from_kernel_cmdline()` now enforces `/etc/kernel/cmdline` as the baseline across discovered BLS entries,
  - per-entry boot-critical metadata is preserved (`root=`, `rootflags=`, `rootfstype=`, `resume=`, and `ro`/`rw` tokens) to keep snapshot-specific entries bootable.
- Improved `opensuse_sdbootutil_update_all_entries()` behavior:
  - now runs direct BLS option synchronization after sdbootutil runs,
  - falls back to direct synchronization when `sdbootutil add-all-kernels` / `update-all-entries` reports errors.
- Extended `regression/custom-kernel-params-regression.sh` call-site wiring checks for the new BLS synchronization helpers.
- Added `kernel_cmdline_persistence_file()` helper to centralize the persisted kernel-cmdline path used by BLS synchronization logic.
- Extended `regression/custom-kernel-params-regression.sh` with runtime BLS synchronization coverage that:
  - creates synthetic BLS entries with snapshot-specific `rootflags=...`,
  - verifies synchronization preserves per-entry `root` / `rootflags` / `rootfstype` / `resume` and `ro`/`rw`,
  - verifies stale per-entry options are removed while persisted baseline tokens are applied.
- Extended `regression/custom-kernel-params-regression.sh` with runtime fallback coverage for `opensuse_sdbootutil_update_all_entries()`:
  - simulates `sdbootutil` command failure via a shim,
  - verifies direct BLS option synchronization fallback still applies persisted kernel-cmdline tokens,
  - verifies per-entry snapshot root metadata remains preserved during fallback synchronization.
- Extended `regression/custom-kernel-params-regression.sh` with an additional partial-failure fallback case for `opensuse_sdbootutil_update_all_entries()`:
  - simulates `sdbootutil add-all-kernels` success followed by `sdbootutil update-all-entries` failure,
  - verifies direct BLS option synchronization fallback still executes and preserves snapshot root metadata while applying persisted baseline options.
- Hardened `is_opensuse_like()` distro detection in `vfio.sh` by parsing `/etc/os-release` key/value pairs directly.
- openSUSE-specific code paths now trigger only when `ID` starts with `opensuse` or an `ID_LIKE` token starts with `opensuse`, reducing accidental matches on non-openSUSE systems.
- Added explicit openSUSE gating diagnostics to `--detect`: the report now prints `openSUSE-like detection` with `yes/no` and the exact reason (`ID` or `ID_LIKE` token match).
- Relaxed startup dependency checks for `--verify`: it no longer requires `modprobe`, allowing read-only verification to run in minimal environments.
- Added LightDM boot-loop mitigation in `vfio.sh` install flow:
  - New preflight detects when LightDM is present but `org.freedesktop.Accounts` (AccountsService) is missing.
  - Offers automatic `accountsservice` installation via detected package manager.
  - Adds optional fallback file `/etc/lightdm/lightdm.conf.d/90-vfio-greeter-fallback.conf` with `greeter-hide-users=true` if installation is unavailable.
- Added `LightDM/AccountsService` status output to `--detect` and `--verify` reports so display-manager dependency health is visible before reboot.
- In `--detect`, when LightDM is present and AccountsService is missing, added an interactive remediation offer that can install `accountsservice` immediately after explicit confirmation.
- Fixed `accountsservice` detection false-positive path: a standalone DBus service file no longer counts as installed unless an actual daemon/service backend is present.
- Added `--detect` remediation for stale `vfio-set-host-audio.service` user units when `/usr/local/bin/vfio-set-host-audio.sh` is missing, including optional interactive cleanup.
- Hardened generated user unit `vfio-set-host-audio.service` with `ConditionPathExists=/usr/local/bin/vfio-set-host-audio.sh` to avoid user-session `203/EXEC` failures after snapshot restore/rollback.
- Added a new `--detect` repair action for legacy `vfio-set-host-audio.service` user units missing the `ConditionPathExists` guard.
- Added automatic install-flow self-heal for legacy `vfio-set-host-audio.service` user units: normal installer runs now auto-repair missing `ConditionPathExists` guards without an extra prompt.
- In `--detect`, when AMD GPUs are present and `vendor-reset` is missing, added an interactive remediation offer that can install `vendor-reset` immediately after explicit confirmation.
- Improved apt-based `vendor-reset` remediation in `--detect`:
  - tries discovered apt package names (via `apt-cache search`) instead of only two fixed package names.
  - when no package is available, offers an explicit opt-in fallback to clone/build/install `vendor-reset` via DKMS from source.
- Refined `vendor-reset` recommendation/install-offer gating:
  - replaced static AMD family matching with evidence-based checks for reset-failure signatures in recent kernel logs.
  - `--detect` now recommends/offers `vendor-reset` only when VFIO/AMD reset-failure markers are observed; otherwise AMD hosts are reported as `N/A`.
- Added optional USB/xHCI stability kernel-parameter prompts in `vfio.sh` for:
  - GRUB path (`grub_add_kernel_params`)
  - openSUSE `/etc/kernel/cmdline` persistence path (`systemd_boot_add_kernel_params`)
  - non-openSUSE systemd-boot current entry updates
- New optional parameters:
  - `usbcore.autosuspend=-1`
  - `pcie_aspm=off`
- Added inline wizard explanations about why the workaround is optional (stability benefit vs idle power tradeoff).
- Updated reset cleanup paths to remove the optional parameters from both `/etc/default/grub` and `/etc/kernel/cmdline`.
- Added a new read-only mode `--usb-health-check` in `vfio.sh`:
  - Scans current and previous boot kernel logs for USB/xHCI crash signatures.
  - Grades results as PASS/WARN/FAIL with exit codes 0/1/2.
  - Prints matching log excerpts and recommends optional mitigation parameters (`usbcore.autosuspend=-1 pcie_aspm=off`) when instability markers are detected.
